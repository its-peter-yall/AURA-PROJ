---
phase: 260414-vw8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AURA-CHAT/client/src/features/chat/components/InlineModelPicker.tsx
  - AURA-CHAT/client/src/features/chat/ChatPage.tsx
autonomous: true
requirements:
  - ADMIN-CONTROL-CHAT-MODEL
must_haves:
  truths:
    - User only sees the admin-selected chat model in the Chat Page dropdown
    - User cannot select a different model in the Chat Page
    - The effective chat model strictly follows the admin's setting
  artifacts:
    - path: AURA-CHAT/client/src/features/chat/components/InlineModelPicker.tsx
      provides: Model restriction via allowedModels prop
    - path: AURA-CHAT/client/src/features/chat/ChatPage.tsx
      provides: Enforced use of config.default_model
  key_links:
    - from: AURA-CHAT/client/src/features/chat/ChatPage.tsx
      to: AURA-CHAT/client/src/features/chat/components/InlineModelPicker.tsx
      via: allowedModels prop binding
---

<objective>
Restrict the Chat Page model dropdown so it only allows the Chat Model selected by the administrator in the Settings Page.
</objective>

<context>
@AURA-CHAT/client/src/features/chat/components/InlineModelPicker.tsx
@AURA-CHAT/client/src/features/chat/ChatPage.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Restrict InlineModelPicker options via allowedModels prop</name>
  <files>AURA-CHAT/client/src/features/chat/components/InlineModelPicker.tsx</files>
  <action>
    - Add an optional `allowedModels?: string[]` property to `InlineModelPickerProps`.
    - Modify the `InlineModelPicker` component to filter the results returned by `useAllModels()`. If `allowedModels` is provided and has elements, only include models whose `name` property is in the `allowedModels` array before passing them to `useGroupedModels()`.
    - If `allowedModels` is provided and has exactly 1 element, disable the dropdown toggle button (remove the onClick handler or set `disabled={true}`) and hide the ChevronDown icon, rendering it as a read-only badge.
  </action>
  <verify>
    <automated>npm --prefix AURA-CHAT/client run test:run -- src/features/chat/components/InlineModelPicker.test.tsx</automated>
  </verify>
  <done>InlineModelPicker accepts an allowedModels prop and restricts its displayed model options correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce admin-selected chat model in ChatPage</name>
  <files>AURA-CHAT/client/src/features/chat/ChatPage.tsx</files>
  <action>
    - Define an `allowedModels` variable derived from the backend chat configuration: `const allowedModels = config?.default_model ? [config.default_model] : undefined;`.
    - Update the `effectiveModel` calculation to enforce strict compliance: evaluate the existing priority (`configState.model || sessionModel || config?.default_model || ''`), but if `allowedModels` exists and the evaluated model is not within it, strictly fallback to `config.default_model`.
    - Pass the `allowedModels` variable down to the `InlineModelPicker` component.
  </action>
  <verify>
    <automated>npm --prefix AURA-CHAT/client run test:run -- src/features/chat/ChatPage.test.tsx</automated>
  </verify>
  <done>ChatPage strictly enforces the admin-configured default_model and restricts the InlineModelPicker options.</done>
</task>
</tasks>
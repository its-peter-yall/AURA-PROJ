---
phase: quick-260415-w80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AURA-NOTES-MANAGER/api/routers/settings.py
  - AURA-NOTES-MANAGER/frontend/src/features/settings/hooks/useSettingsApi.ts
  - AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx
  - AURA-NOTES-MANAGER/frontend/src/pages/SettingsPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings default-model dropdown shows selectable models instead of empty state."
    - "Returning to the Settings browser tab does not reset visible dropdown state or feel like a page refresh."
    - "Model list remains usable even when one provider is unavailable or unauthorized."
  artifacts:
    - path: "AURA-NOTES-MANAGER/api/routers/settings.py"
      provides: "Resilient model aggregation for /api/v1/settings/models"
    - path: "AURA-NOTES-MANAGER/frontend/src/features/settings/hooks/useSettingsApi.ts"
      provides: "Stable query cache/focus behavior for settings model/default data"
    - path: "AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx"
      provides: "Dropdown rendering that preserves last valid model list/selection"
    - path: "AURA-NOTES-MANAGER/frontend/src/pages/SettingsPage.tsx"
      provides: "Health query config that avoids focus-triggered UI reset sensation"
  key_links:
    - from: "api/routers/settings.py:list_all_models"
      to: "DefaultModelSection model picker"
      via: "GET /api/v1/settings/models response"
      pattern: "models.extend|except"
    - from: "useSettingsApi.ts useAllModels/useDefaults"
      to: "DefaultModelSection loading and picker state"
      via: "TanStack Query cache + refetch options"
      pattern: "gcTime|staleTime|refetchOnWindowFocus|placeholderData"
---

<objective>
Fix two user-reported Settings regressions in one focused pass:
1) model dropdown shows no models,
2) switching away and back causes refresh/reset feeling.

Purpose: keep Settings model selection reliable and stable for admin workflows.
Output: resilient model feed + non-jarring Settings tab revisit behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AURA-NOTES-MANAGER/api/routers/settings.py
@AURA-NOTES-MANAGER/frontend/src/features/settings/hooks/useSettingsApi.ts
@AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx
@AURA-NOTES-MANAGER/frontend/src/pages/SettingsPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make /settings/models return partial-success results instead of failing whole list</name>
  <files>AURA-NOTES-MANAGER/api/routers/settings.py</files>
  <behavior>
    - If one provider (e.g., openrouter) errors/auth-fails, endpoint still returns models from healthy providers.
    - Endpoint only returns empty list when all providers are unavailable, not when a single provider fails.
    - Server logs provider-specific failures for diagnosability without breaking Settings dropdown population.
  </behavior>
  <action>
Adjust `list_all_models` aggregation to tolerate per-provider `ModelRouterError` failures (continue collecting from other providers) while preserving current JSON model schema. Keep strict behavior for clearly unrecoverable errors only if no provider succeeded. This directly addresses complaint (1) where dropdown appears empty because one provider failure currently aborts the full response.
  </action>
  <verify>
    <automated>./.venv/Scripts/python -m pytest AURA-NOTES-MANAGER/api/tests -k settings -q</automated>
  </verify>
  <done>
    - `/api/v1/settings/models` no longer hard-fails when a single provider is down.
    - Healthy-provider models are still returned and consumable by frontend picker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize Settings model/default queries to prevent empty flashes and tab-return resets</name>
  <files>AURA-NOTES-MANAGER/frontend/src/features/settings/hooks/useSettingsApi.ts, AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx</files>
  <behavior>
    - Revisiting browser tab keeps previous model list visible while background refetch happens.
    - Local selected model does not get overwritten by transient empty/default-loading values.
    - Dropdown does not regress to “No models available” during focus/reconnect transitions.
  </behavior>
  <action>
In `useSettingsApi`, explicitly configure settings queries (`useAllModels`, `useDefaults`) for focus-stable behavior (`refetchOnWindowFocus: false`, `refetchOnReconnect: false` where appropriate, and `placeholderData`/equivalent keep-previous-data strategy). In `DefaultModelSection`, guard state synchronization so only valid non-empty server defaults replace local selection, and avoid rendering an empty-model state during transient refetch when prior data exists. This addresses complaints (1) and (2) at UI data-lifecycle level.
  </action>
  <verify>
    <automated>cd AURA-NOTES-MANAGER/frontend && npm run build && npm test -- --run</automated>
  </verify>
  <done>
    - Returning to Settings tab preserves model dropdown contents and selected value.
    - Refetches happen without visible state reset.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tune SettingsPage health query to remove page-refresh perception on tab revisit</name>
  <files>AURA-NOTES-MANAGER/frontend/src/pages/SettingsPage.tsx</files>
  <action>
Configure the health query with explicit non-jarring focus behavior (`refetchOnWindowFocus: false`, sensible `staleTime`, and preserve-last-data semantics) so revisiting tab does not trigger broad UI churn that feels like a page refresh. Keep manual refresh button behavior intact. This specifically addresses complaint (2) about unwanted refresh/reset when revisiting Settings.
  </action>
  <verify>
    <automated>cd AURA-NOTES-MANAGER/frontend && npm run build</automated>
  </verify>
  <done>
    - Settings page no longer appears to reload/reset when user leaves and returns to tab.
    - System Status can still be manually refreshed via existing button.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → settings API | Untrusted client requests model/settings data |
| settings API → external providers | Upstream provider failures/auth errors can impact response shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-w80-01 | D | `list_all_models` provider loop | mitigate | Continue on per-provider failure; avoid full endpoint failure for single-provider outage |
| T-quick-w80-02 | I | Frontend transient empty states | mitigate | Preserve prior valid cache data during refetch; prevent misleading empty dropdown UI |
| T-quick-w80-03 | R | Settings health/model refresh behavior | mitigate | Deterministic query options and explicit refresh controls reduce inconsistent UX states |
</threat_model>

<verification>
1. Run backend tests for settings routes.
2. Run frontend build and unit tests.
3. Manual smoke: open Settings, confirm models are visible, switch tabs/apps, return, confirm no reset and selected model remains.
</verification>

<success_criteria>
- [ ] Complaint 1 resolved: Settings dropdown shows models reliably.
- [ ] Complaint 2 resolved: tab revisit no longer feels like full page refresh/reset.
- [ ] Backend model listing is resilient to single-provider failures.
- [ ] Frontend build/tests pass with no new regressions.
</success_criteria>

<output>
After completion, create `.planning/quick/260415-w80-fix-settings-page-model-dropdown-showing/260415-w80-SUMMARY.md`
</output>

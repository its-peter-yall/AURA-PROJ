---
phase: quick
plan: "260409-m79"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/model_router/src/model_router/types.py
  - shared/model_router/src/model_router/providers/openrouter.py
  - shared/model_router/tests/test_openrouter_provider.py
  - AURA-CHAT/server/routers/chat.py
  - AURA-CHAT/client/src/types/api.ts
  - AURA-CHAT/client/src/features/chat/components/InputArea.tsx
  - AURA-CHAT/client/src/features/chat/components/InputArea.test.tsx
  - AURA-CHAT/client/src/features/chat/ChatPage.tsx
  - AURA-CHAT/client/src/features/chat/ChatPage.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Thinking toggle behavior reflects model capability for both Vertex and OpenRouter models."
    - "OpenRouter models that can return reasoning/thinking are surfaced as thinking-capable in chat config."
    - "Unsupported models never send enable_thinking=true requests from the UI."
  artifacts:
    - path: "shared/model_router/src/model_router/types.py"
      provides: "Model capability contract on ModelInfo"
    - path: "AURA-CHAT/server/routers/chat.py"
      provides: "Capability-aware /chat/config response for thinking models"
    - path: "AURA-CHAT/client/src/features/chat/components/InputArea.tsx"
      provides: "Option-3 thinking toggle states in chat input"
  key_links:
    - from: "shared/model_router/src/model_router/providers/openrouter.py"
      to: "AURA-CHAT/server/routers/chat.py"
      via: "router.list_models() capability metadata"
      pattern: "thinking_supported|capabilities"
    - from: "AURA-CHAT/server/routers/chat.py"
      to: "AURA-CHAT/client/src/features/chat/components/InputArea.tsx"
      via: "chat config thinking payload"
      pattern: "thinking\.supported_models|thinking\.model_capabilities"
---

<objective>
Implement option 3 for thinking-toggle states by driving UI behavior from model capability metadata, including OpenRouter models.

Purpose: Remove hardcoded/Vertex-only thinking support assumptions and keep toggle behavior accurate as models/providers change.
Output: Capability-aware model metadata flow (router → /chat/config → chat UI) with tests covering OpenRouter and UI state handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@AURA-CHAT/server/routers/chat.py
@AURA-CHAT/backend/utils/config.py
@AURA-CHAT/client/src/features/chat/components/InputArea.tsx
@AURA-CHAT/client/src/features/chat/ChatPage.tsx
@shared/model_router/src/model_router/types.py
@shared/model_router/src/model_router/providers/openrouter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thinking-capability metadata for OpenRouter models in shared router</name>
  <files>shared/model_router/src/model_router/types.py, shared/model_router/src/model_router/providers/openrouter.py, shared/model_router/tests/test_openrouter_provider.py</files>
  <behavior>
    - Test 1: OpenRouter model IDs known to support reasoning return ModelInfo entries marked as thinking-capable.
    - Test 2: OpenRouter models without reasoning support are marked non-thinking-capable.
    - Test 3: Existing list_models() behavior (curated providers, model naming) remains intact.
  </behavior>
  <action>
    Extend `ModelInfo` with capability metadata (for example a `thinking_supported` boolean or `capabilities` object) and populate it in `OpenRouterProvider.list_models()` using deterministic model-name rules aligned with existing thinking translation logic (`_build_thinking_params`). Keep capability inference explicit and local to provider code (no network-side schema assumptions beyond current `/models` payload). Add/adjust provider tests to lock the new metadata contract.
  </action>
  <verify>
    <automated>python -m pytest shared/model_router/tests/test_openrouter_provider.py -q</automated>
  </verify>
  <done>ModelInfo returned by OpenRouter provider includes stable thinking capability metadata with passing provider tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make /chat/config emit capability-aware thinking support</name>
  <files>AURA-CHAT/server/routers/chat.py</files>
  <behavior>
    - Test 1: /chat/config derives `thinking.supported_models` from dynamic router model metadata when available.
    - Test 2: /chat/config keeps resilient fallback to `config.CHAT_MODELS_WITH_THINKING` if router model listing/capability extraction fails.
    - Test 3: Response remains backward-compatible for existing frontend consumers.
  </behavior>
  <action>
    Update `get_config()` to compute thinking support from `router.list_models()` capability metadata (including OpenRouter), return `thinking.supported_models` from that set, and add a lightweight `thinking.model_capabilities` map keyed by model id for frontend state logic. Preserve current fallback path and never break when router/Redis/OpenRouter metadata is unavailable.
  </action>
  <verify>
    <automated>python -m pytest AURA-CHAT/tests/api -k chat -q</automated>
  </verify>
  <done>Chat config endpoint returns capability-aware thinking metadata with robust fallback behavior and no regression to allowed_models/default_model fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement option-3 thinking toggle states in chat UI</name>
  <files>AURA-CHAT/client/src/types/api.ts, AURA-CHAT/client/src/features/chat/components/InputArea.tsx, AURA-CHAT/client/src/features/chat/components/InputArea.test.tsx, AURA-CHAT/client/src/features/chat/ChatPage.tsx, AURA-CHAT/client/src/features/chat/ChatPage.test.tsx</files>
  <behavior>
    - Test 1: Toggle is interactive when selected model is marked thinking-capable.
    - Test 2: Toggle renders disabled/non-interactive state for non-capable selected models (option 3 behavior) while preserving clear affordance.
    - Test 3: Requests sent from ChatPage force `enable_thinking=false` when model is not capable, regardless of previous toggle state.
  </behavior>
  <action>
    Update chat config types to include model capability metadata and apply option-3 state handling in both `InputArea` and `ChatPage`: show enabled/disabled visual states based on capability, prevent unsupported toggles from mutating state, and sanitize outgoing request payload so unsupported models never request thinking. Update unit tests to cover OpenRouter-capable and non-capable model transitions, including model-switch edge cases.
  </action>
  <verify>
    <automated>npm --prefix AURA-CHAT/client run test -- src/features/chat/components/InputArea.test.tsx src/features/chat/ChatPage.test.tsx</automated>
  </verify>
  <done>Thinking toggle UI state and request payload behavior are capability-driven, deterministic, and covered by frontend tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/chat/config` | Untrusted clients consume capability metadata that controls UI behavior. |
| Server → model_router provider metadata | External provider model metadata influences server-declared feature support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260409-m79-01 | T (Tampering) | `AURA-CHAT/server/routers/chat.py` config shaping | mitigate | Derive thinking support from trusted server-side router metadata only; never trust client claims for capability. |
| T-260409-m79-02 | D (Denial of Service) | `/chat/config` dependency on router/model listing | mitigate | Keep existing exception fallback to static config lists so config endpoint stays available if provider listing fails. |
| T-260409-m79-03 | I (Information Disclosure) | capability payload | accept | Capability flags expose only model feature support (no secrets/keys/usage data), acceptable low-risk metadata. |
</threat_model>

<verification>
- `python -m pytest shared/model_router/tests/test_openrouter_provider.py -q`
- `python -m pytest AURA-CHAT/tests/api -k chat -q`
- `npm --prefix AURA-CHAT/client run test -- src/features/chat/components/InputArea.test.tsx src/features/chat/ChatPage.test.tsx`
</verification>

<success_criteria>
- `/chat/config` thinking support includes OpenRouter-capable models via router metadata (with fallback safety).
- Chat input applies option-3 thinking toggle states correctly for capable vs non-capable models.
- No unsupported model can send `enable_thinking=true` from the chat UI.
</success_criteria>

<output>
After completion, create `.planning/quick/260409-m79-implement-option-3-for-thinking-toggle-s/260409-m79-SUMMARY.md`
</output>

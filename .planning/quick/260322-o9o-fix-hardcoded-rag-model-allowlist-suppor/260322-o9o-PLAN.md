---
phase: quick
plan: 260322-o9o
type: execute
wave: 1
depends_on: []
files_modified:
  - AURA-CHAT/backend/rag_engine.py
  - AURA-CHAT/server/routers/chat.py
  - AURA-CHAT/tests/backend/test_rag_engine_response_parsing.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Any model the model_router can resolve (Vertex AI, OpenRouter) is accepted by set_model()"
    - "No model is rejected solely because it is absent from a hardcoded allowlist"
    - "When no model is specified, set_model() falls back to RAG_MODEL_DEFAULT"
    - "Truly invalid model names still trigger a graceful fallback to the default"
  artifacts:
    - path: "AURA-CHAT/backend/rag_engine.py"
      provides: "set_model() without hardcoded allowlist gate"
      min_lines: 45
  key_links:
    - from: "AURA-CHAT/server/routers/chat.py:334"
      to: "AURA-CHAT/backend/rag_engine.py:set_model()"
      via: "rag_engine.set_model(request.model)"
      pattern: "rag_engine\\.set_model\\(request\\.model\\)"
    - from: "AURA-CHAT/backend/rag_engine.py:set_model()"
      to: "AURA-CHAT/backend/utils/vertex_ai_client.py:get_model()"
      via: "get_model(requested)"
      pattern: "self\\.model = get_model\\(requested\\)"
---

<objective>
Remove the hardcoded RAG model allowlist so any provider/model the model_router supports is usable in the chat.

Purpose: Users pick models from the admin settings page (which lists ALL models from ALL providers via `/api/v1/settings/models`), but `set_model()` in `rag_engine.py` silently rejects anything not in a hardcoded 3-model list. This blocks OpenRouter models like `anthropic/claude-sonnet-4` and any newly added Vertex AI models.

Output: Updated `rag_engine.py` with allowlist gate removed; updated `/config` endpoint to return dynamic model list; cleaned-up test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded allowlist from set_model()</name>
  <files>AURA-CHAT/backend/rag_engine.py</files>
  <action>
    Replace the allowlist validation in `set_model()` (lines 183-201) with a pass-through that trusts the model_router.

    Current logic (lines 183-201):
    ```python
    allowed_models = getattr(config, "RAG_ALLOWED_MODELS", None) or ["gemini-2.5-flash-lite"]
    configured_default = getattr(config, "RAG_MODEL_DEFAULT", None) or "gemini-2.5-flash-lite"
    default_model = configured_default if configured_default in allowed_models else allowed_models[0]
    requested = (model_name or default_model).strip()
    if requested not in allowed_models:
        self.model_error = f"Unknown model '{requested}'. Falling back to '{default_model}'."
        logger.warning(self.model_error)
        requested = default_model
    ```

    New logic — remove the `allowed_models` variable entirely and simplify to:
    ```python
    default_model = getattr(config, "RAG_MODEL_DEFAULT", None) or "gemini-2.5-flash-lite"
    requested = (model_name or default_model).strip()
    ```

    Keep the existing try/except block (lines 203-228) which already handles real failures from `get_model()` — if the model router raises `ModelUnavailableError` or any other exception, it falls back to `default_model`. This is the correct validation gate; the hardcoded allowlist was a redundant, overly-restrictive duplicate.

    Do NOT remove `RAG_ALLOWED_MODELS` from `config.py` — it is still referenced by the `/config` endpoint in `chat.py` and by tests. That cleanup is a separate concern.
  </action>
  <verify>
    <automated>cd "D:\Peter\AURA Twin Proj\AURA-PROJ" && .venv\Scripts\python -m pytest AURA-CHAT/tests/backend/test_rag_engine_response_parsing.py -x -q</automated>
  </verify>
  <done>
    set_model() accepts any model name without rejecting it based on a hardcoded list.
    The try/except around get_model() still provides the fallback for truly invalid models.
    Passing a valid model (e.g. "gemini-2.5-flash") returns that model name.
    Passing an invalid model falls back to RAG_MODEL_DEFAULT via the exception handler.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make /config endpoint use dynamic model list</name>
  <files>AURA-CHAT/server/routers/chat.py</files>
  <action>
    Update the `GET /config` endpoint (lines 293-306) so `allowed_models` returns the dynamic model list from the model router instead of the hardcoded `config.RAG_ALLOWED_MODELS`.

    Current:
    ```python
    return {
        "allowed_models": config.RAG_ALLOWED_MODELS,
        ...
    }
    ```

    Replace `config.RAG_ALLOWED_MODELS` with a dynamic lookup. Import `get_model` is not the right approach here — instead, use the same model cache that `/api/v1/settings/models` uses. Look at how `settings.py` imports `ModelCache` and `get_model_cache`.

    If that import chain is too heavy for this endpoint (it likely requires Redis), fall back to a simpler approach: remove `allowed_models` from the `/config` response entirely. The frontend already uses `useAllModels()` which calls `/api/v1/settings/models` for the model picker. The `/config` `allowed_models` field is only consumed as a fallback — verify by grepping the client for `allowed_models`.

    Preferred approach (simpler): Remove `allowed_models` from the `/config` response and update the `default_model` to still come from `config.RAG_MODEL_DEFAULT`. Keep the `thinking` section unchanged.

    After updating, grep the client for any usage of `allowed_models` from `/config` response to confirm no frontend breakage.
  </action>
  <verify>
    <automated>cd "D:\Peter\AURA Twin Proj\AURA-PROJ" && .venv\Scripts\python -m pytest AURA-CHAT/tests/api/test_chat.py -x -q -k config</automated>
  </verify>
  <done>
    /config endpoint no longer returns hardcoded RAG_ALLOWED_MODELS.
    Frontend model picker continues to work via useAllModels() → /api/v1/settings/models.
    No client-side references to allowed_models from /config are broken.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update allowlist test to verify pass-through</name>
  <files>AURA-CHAT/tests/backend/test_rag_engine_response_parsing.py</files>
  <behavior>
    - Test that set_model() accepts a model name NOT in the old RAG_ALLOWED_MODELS
    - Test that set_model() falls back to default when get_model() raises an exception
    - Remove or update the assertion that RAG_ALLOWED_MODELS must be defined
  </behavior>
  <action>
    Find the test at line ~125 that asserts `RAG_ALLOWED_MODELS` must be defined. Update it to test the new behavior:

    1. Test: `set_model("any-valid-model-name")` succeeds (returns the name, no model_error)
    2. Test: `set_model(None)` falls back to `RAG_MODEL_DEFAULT`
    3. Remove the assertion `self.assertTrue(allowed_models, "RAG_ALLOWED_MODELS must be defined")` since the allowlist is no longer the validation mechanism

    If the test file uses unittest-style classes, maintain that pattern. If it uses pytest, use pytest style.
  </action>
  <verify>
    <automated>cd "D:\Peter\AURA Twin Proj\AURA-PROJ" && .venv\Scripts\python -m pytest AURA-CHAT/tests/backend/test_rag_engine_response_parsing.py -x -q</automated>
  </verify>
  <done>
    Tests verify set_model() pass-through behavior instead of allowlist membership.
    No test asserts RAG_ALLOWED_MODELS must exist.
    All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. Run full backend test suite: `python -m pytest AURA-CHAT/tests/ -x -q`
2. Confirm no references to `RAG_ALLOWED_MODELS` validation remain in `rag_engine.py`
3. Confirm `get_model()` try/except is the sole validation gate
</verification>

<success_criteria>
- set_model() accepts any model name string without hardcoded list check
- Model router (via get_model()) is the single source of truth for model availability
- /config endpoint returns dynamic model data or no longer includes hardcoded allowed_models
- All backend tests pass
- Frontend model picker still shows all available models from /api/v1/settings/models
</success_criteria>

<output>
After completion, create `.planning/quick/260322-o9o-fix-hardcoded-rag-model-allowlist-suppor/260322-o9o-SUMMARY.md`
</output>

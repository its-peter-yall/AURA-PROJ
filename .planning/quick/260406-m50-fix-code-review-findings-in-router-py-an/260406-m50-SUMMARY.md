---
phase: quick
plan: "260406-m50"
subsystem: model_router / infrastructure
tags: [code-review, router, batch-script, fix]
dependency_graph:
  requires: []
  provides: []
  affects: [shared/model_router, AURA-PROJ/infrastructure]
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - shared/model_router/src/model_router/router.py
    - run-all.bat
decisions: []
metrics:
  duration: "~2 min"
  completed: "2026-04-06T16:00:17Z"
---

# Quick Task 260406-m50: Fix Code Review Findings - Summary

## One-Liner
Fixed 4 code review findings in router.py (try/except wrapping, informative error, variable rename, else removal) and 3 findings in run-all.bat (taskkill replacing WMIC, bounded Redis loop, dead timeout removal).

## Tasks Completed

| # | Task | Commit | Status |
|---|------|--------|--------|
| 1 | Fix router.py code review findings (4 fixes) | `e5f6da0` | Done |
| 2 | Fix run-all.bat code review findings (3 fixes) | `e4f801c` | Done |

## Deviations from Plan

None - plan executed exactly as written.

## Commits

| Hash | Message |
|------|---------|
| `e5f6da0` | fix(quick-260406-m50): address 4 code review findings in router.py |
| `e4f801c` | fix(quick-260406-m50): address 3 code review findings in run-all.bat |

## router.py Fixes Applied

1. **Fix 1 (line 296):** Wrap `_coerce_provider_type(provider)` in try/except ValueError, re-raising original error on failure
2. **Fix 2 (after line 298):** Lazy registration failure now raises `ModelUnavailableError` with informative message about API key context
3. **Fix 3 (line 293):** Renamed `is_invalid_provider` to `is_valid_provider_type` (eliminates double negative logic)
4. **Fix 4 (lines 304-305):** Removed redundant else branch, `raise err` moved outside the if block

## run-all.bat Fixes Applied

1. **Fix 3 (lines 5-7):** Replaced deprecated WMIC with `taskkill /F /PID %%a`
2. **Fix 7 (line 14):** Removed dead `timeout /t 2 /nobreak >nul` line
3. **Fix 4 (line 17):** Replaced unbounded `until redis-cli ping` loop with bounded 10-iteration `for /L` loop

## Verification

- router.py: `python -m py_compile` passes ✓
- run-all.bat: File exists, no WMIC references ✓

## Self-Check: PASSED

- Commits `e5f6da0` and `e4f801c` verified in git log
- Files `shared/model_router/src/model_router/router.py` and `run-all.bat` confirmed modified

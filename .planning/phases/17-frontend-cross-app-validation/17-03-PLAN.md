---
phase: 17-frontend-cross-app-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 17-02
files_modified:
  - AURA-NOTES-MANAGER/frontend/e2e/settings.spec.ts
  - AURA-CHAT/client/e2e/settings.spec.ts
autonomous: true
requirements:
  - PP-01
  - PP-02
  - PP-03
  - PP-04
  - PP-05
  - PP-06
  - PP-07
  - PP-08
  - FB-01
  - FB-02
user_setup: []
must_haves:
  truths:
    - "NOTES-MANAGER settings E2E test verifies all 5 use case rows are visible on the settings page"
    - "NOTES-MANAGER settings E2E test verifies gatekeeper model picker is functional"
    - "CHAT settings E2E test verifies all 5 use case rows are visible on the settings page"
    - "CHAT settings E2E test verifies settings page is admin-only (non-admin redirected)"
    - "AST audit passes — no direct SDK imports outside shared/model_router/"
    - "Both applications pass their full test suites"
  artifacts:
    - path: "AURA-NOTES-MANAGER/frontend/e2e/settings.spec.ts"
      provides: "Playwright E2E tests for NOTES-MANAGER settings page"
      contains: "gatekeeper"
    - path: "AURA-CHAT/client/e2e/settings.spec.ts"
      provides: "Playwright E2E tests for AURA-CHAT settings page"
      contains: "gatekeeper"
  key_links:
    - from: "settings.spec.ts"
      to: "SettingsPage"
      via: "page.goto('/settings') and locators"
      pattern: "page\\.goto.*settings"
    - from: "settings.spec.ts"
      to: "/api/v1/settings/defaults"
      via: "API route mocking for offline testing"
      pattern: "page\\.route.*settings/defaults"
---

<objective>
Create Playwright E2E test specs for both applications' settings pages and run the AST audit to verify no direct SDK imports exist outside shared/model_router/.

Purpose: Validate that:
1. All 5 use case rows (including gatekeeper and relationship_extraction) are visible and functional in both settings pages
2. AURA-CHAT settings is admin-only (non-admins are redirected)
3. No forbidden SDK imports exist (AST audit)
4. Both apps' full test suites pass with the new settings code

Output: 2 new E2E test spec files + confirmed passing AST audit + confirmed passing full test suites.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-frontend-cross-app-validation/17-UI-SPEC.md
@.planning/phases/17-frontend-cross-app-validation/17-RESEARCH.md
@tests/test_no_direct_imports.py

<interfaces>
From AURA-NOTES-MANAGER/frontend/e2e/fixtures.ts (E2E fixture pattern):
```typescript
// Uses Playwright test fixture with baseURL, auth setup, etc.
// Settings tests should use same fixture pattern
```

From 17-RESEARCH.md Playwright mock pattern:
```typescript
await page.route('**/api/v1/settings/defaults', async (route) => {
    await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
            chat: { provider: 'vertex_ai', model: 'gemini-2.5-flash-lite' },
            embeddings: { provider: 'vertex_ai', model: 'text-embedding-004' },
            entity_extraction: { provider: 'vertex_ai', model: 'gemini-2.5-flash-lite' },
            gatekeeper: { provider: 'vertex_ai', model: 'gemini-2.5-flash-lite' },
            relationship_extraction: { provider: 'vertex_ai', model: 'gemini-2.5-flash-lite' },
        }),
    });
});
```

From tests/test_no_direct_imports.py (existing AST audit — SCAN_DIRS):
```python
SCAN_DIRS = {
    "AURA-CHAT": ["AURA-CHAT/backend", "AURA-CHAT/server"],
    "AURA-NOTES-MANAGER": ["AURA-NOTES-MANAGER/api", "AURA-NOTES-MANAGER/services"],
}
# Runs: python -m pytest tests/test_no_direct_imports.py -v
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NOTES-MANAGER settings E2E test spec</name>
  <read_first>
    AURA-NOTES-MANAGER/frontend/e2e/fixtures.ts
    AURA-NOTES-MANAGER/frontend/playwright.config.ts
    AURA-NOTES-MANAGER/frontend/src/pages/SettingsPage.tsx
    AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx
  </read_first>
  <files>AURA-NOTES-MANAGER/frontend/e2e/settings.spec.ts</files>
  <action>
    Create `AURA-NOTES-MANAGER/frontend/e2e/settings.spec.ts` with file header. Use the existing Playwright fixture from `./fixtures`.

    Test suite: "Settings Page"

    **Test 1: "displays all 5 use case rows"**
    - Navigate to /settings
    - Mock `/api/v1/settings/defaults` and `/api/v1/settings/models` routes with realistic data (include all 5 use cases)
    - Assert all 5 use case labels are visible: "Chat Model", "Embeddings Model", "Entity Extraction Model", "Gatekeeper Model", "Relationship Extraction Model"

    **Test 2: "gatekeeper model picker is functional"**
    - Navigate to /settings with mocked API data
    - Locate the picker for the Gatekeeper Model row (find by heading text "Gatekeeper Model")
    - Click the picker trigger
    - Verify the dropdown opens and shows model options
    - Select a model
    - Verify the mutation was called (check that PUT `/api/v1/settings/defaults/gatekeeper` was called)

    **Test 3: "relationship extraction model picker is functional"**
    - Same as Test 2 but for "Relationship Extraction Model"

    Mock data for `/api/v1/settings/defaults`:
    ```json
    {
        "chat": { "provider": "vertex_ai", "model": "gemini-2.5-flash-lite" },
        "embeddings": { "provider": "vertex_ai", "model": "text-embedding-004" },
        "entity_extraction": { "provider": "vertex_ai", "model": "gemini-2.5-flash-lite" },
        "gatekeeper": { "provider": "vertex_ai", "model": "gemini-2.5-flash-lite" },
        "relationship_extraction": { "provider": "vertex_ai", "model": "gemini-2.5-flash-lite" }
    }
    ```

    Mock data for `/api/v1/settings/models` (at least 3 models from different providers):
    ```json
    [
        { "name": "gemini-2.5-flash-lite", "provider": "vertex_ai", "display_name": "Gemini 2.5 Flash Lite", "model_type": "generation" },
        { "name": "text-embedding-004", "provider": "vertex_ai", "display_name": "Text Embedding 004", "model_type": "embedding" },
        { "name": "anthropic/claude-sonnet-4", "provider": "openrouter", "display_name": "Claude Sonnet 4", "model_type": "generation" }
    ]
    ```
  </action>
  <verify>
    ```bash
    cd AURA-NOTES-MANAGER/frontend && npx playwright test e2e/settings.spec.ts --reporter=list
    ```
  </verify>
  <acceptance_criteria>
    - File AURA-NOTES-MANAGER/frontend/e2e/settings.spec.ts exists with 3 tests
    - Test "displays all 5 use case rows" passes — locators find all 5 labels
    - Test "gatekeeper model picker is functional" passes — picker opens and model can be selected
    - Test "relationship extraction model picker is functional" passes — picker opens and model can be selected
    - All API routes are mocked (no live backend required)
    - `npx playwright test e2e/settings.spec.ts` exits with code 0
  </acceptance_criteria>
  <done>NOTES-MANAGER settings E2E test spec created and passing</done>
</task>

<task type="auto">
  <name>Task 2: Create AURA-CHAT settings E2E test spec</name>
  <read_first>
    AURA-CHAT/client/e2e/chat.spec.ts
    AURA-CHAT/client/playwright.config.ts
    AURA-CHAT/client/src/features/settings/SettingsPage.tsx
    AURA-CHAT/client/src/features/settings/components/DefaultModelSection.tsx
  </read_first>
  <files>AURA-CHAT/client/e2e/settings.spec.ts</files>
  <action>
    Create `AURA-CHAT/client/e2e/settings.spec.ts` with file header. Use AURA-CHAT's Playwright test fixture pattern (from chat.spec.ts).

    Test suite: "Settings Page"

    **Test 1: "displays all 5 use case rows"**
    - Navigate to /settings
    - Mock `/api/v1/settings/defaults` and `/api/v1/settings/models` routes with realistic data (same mock data structure as NOTES test)
    - Assert all 5 use case labels are visible: "Chat Model", "Embeddings Model", "Entity Extraction Model", "Gatekeeper Model", "Relationship Extraction Model"

    **Test 2: "gatekeeper model picker is functional"**
    - Same pattern as NOTES-MANAGER test 2
    - Locate gatekeeper picker, click, select model, verify mutation

    **Test 3: "settings page is admin-only"**
    - Navigate to /settings without admin auth (or mock auth store to return non-admin)
    - Verify redirect to / (non-admin should not access settings)
    - This test may require mocking the auth store state

    Use the same mock data structure as the NOTES-MANAGER test for `/api/v1/settings/defaults` and `/api/v1/settings/models`.
  </action>
  <verify>
    ```bash
    cd AURA-CHAT/client && npx playwright test e2e/settings.spec.ts --reporter=list
    ```
  </verify>
  <acceptance_criteria>
    - File AURA-CHAT/client/e2e/settings.spec.ts exists with 3 tests
    - Test "displays all 5 use case rows" passes
    - Test "gatekeeper model picker is functional" passes
    - Test "settings page is admin-only" passes
    - All API routes are mocked
    - `npx playwright test e2e/settings.spec.ts` exits with code 0
  </acceptance_criteria>
  <done>AURA-CHAT settings E2E test spec created and passing</done>
</task>

<task type="auto">
  <name>Task 3: Run AST audit and full test suites</name>
  <read_first>tests/test_no_direct_imports.py</read_first>
  <files></files>
  <action>
    Run the AST audit and both applications' full test suites:

    1. **AST audit** (from project root with venv):
    ```bash
    python -m pytest tests/test_no_direct_imports.py -v
    ```

    2. **AURA-CHAT client build + lint + tests**:
    ```bash
    cd AURA-CHAT/client && npm run build
    cd AURA-CHAT/client && npm run lint
    cd AURA-CHAT/client && npm run test
    ```

    3. **AURA-NOTES-MANAGER frontend build + lint + tests**:
    ```bash
    cd AURA-NOTES-MANAGER/frontend && npm run build
    cd AURA-NOTES-MANAGER/frontend && npm run lint
    cd AURA-NOTES-MANAGER/frontend && npm run test
    ```

    4. **Backend tests** (from project root with venv):
    ```bash
    python -m pytest AURA-CHAT/tests/ -v
    python -m pytest AURA-NOTES-MANAGER/api/tests/ -v
    ```

    Report all results. If any test fails, document the failure and whether it's pre-existing or new.
  </action>
  <verify>
    All test commands exit with code 0
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_no_direct_imports.py -v` exits 0 — no forbidden imports found
    - `npm run build` in AURA-CHAT/client exits 0
    - `npm run build` in AURA-NOTES-MANAGER/frontend exits 0
    - `npm run lint` in AURA-CHAT/client exits 0
    - `npm run lint` in AURA-NOTES-MANAGER/frontend exits 0
    - `npm run test` in AURA-CHAT/client exits 0
    - `npm run test` in AURA-NOTES-MANAGER/frontend exits 0
    - `python -m pytest AURA-CHAT/tests/ -v` exits 0
    - `python -m pytest AURA-NOTES-MANAGER/api/tests/ -v` exits 0
    - Any pre-existing test failures are documented in the summary
  </acceptance_criteria>
  <done>AST audit clean, both applications pass full test suites</done>
</task>

</tasks>

<verification>
1. AST audit: `python -m pytest tests/test_no_direct_imports.py -v` — all 11 tests pass
2. AURA-CHAT: `cd AURA-CHAT/client && npm run build && npm run lint && npm run test` — all pass
3. AURA-NOTES-MANAGER: `cd AURA-NOTES-MANAGER/frontend && npm run build && npm run lint && npm run test` — all pass
4. E2E: `npx playwright test e2e/settings.spec.ts` — all pass in both apps
5. Backend: `python -m pytest AURA-CHAT/tests/ -v && python -m pytest AURA-NOTES-MANAGER/api/tests/ -v` — all pass
</verification>

<success_criteria>
- 2 new E2E test spec files created (6 total tests across both apps)
- All E2E tests pass with mocked API data
- AST audit confirms no direct SDK imports outside shared/model_router/
- Both applications' full test suites pass (unit + E2E + backend)
- No new lint errors or type errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/17-frontend-cross-app-validation/17-03-SUMMARY.md`
</output>

<objective>
Create Playwright E2E tests for critical user flows in both AURA-CHAT and AURA-NOTES-MANAGER.
</objective>

<context>
@.planning/ROADMAP.md - Phase 7 (Testing & Optimization)
@AURA-CHAT/client/e2e/ - Existing E2E specs (chat.spec.ts, graph.spec.ts, documents.spec.ts, health.spec.ts)
@AURA-NOTES-MANAGER/frontend/ - E2E setup with fixtures (explorer.spec.ts, kg-processing.spec.ts, health.spec.ts)

Previous: Phase 7-03 AURA-NOTES-MANAGER Unit Tests
Current: Phase 7-04 Integration & E2E Tests
</context>

<requirements>
1. Expand existing AURA-CHAT E2E tests with error handling and edge cases
2. Create AURA-NOTES-MANAGER E2E tests with comprehensive CRUD flows
3. Test critical user flows (CRUD operations, navigation, selection modes)
4. Test error scenarios and edge cases (@edge, @regression)
5. Mock API responses for fast, reliable tests using fixture patterns
6. Use Playwright test annotations for organization (@critical, @smoke, @crud, @regression)
7. Test across all browsers (Chromium, Firefox, WebKit) and mobile devices
8. Add performance E2E tests for load time and resource monitoring
9. Integrate with CI/CD pipelines for automated test execution
10. Follow existing code patterns from chat.spec.ts and graph.spec.ts
</requirements>

<test_annotations>
@critical - Core user journeys (login, chat, graph, CRUD operations)
@smoke - Quick sanity tests for CI pipelines
@crud - Create, Read, Update, Delete operations
@edge - Error handling, boundary conditions, timeouts
@regression - Regression test suites for bug fixes
@navigation - Page navigation, routing, responsive layouts
@slow - Tests requiring live LLM credentials or long timeouts
@performance - Performance tests (load time, memory, resource monitoring)
@mobile - Mobile-specific tests and responsive behavior
</test_annotations>

<mobile_viewport_config>
devices:
  # iOS Devices
  - name: "iPhone 12"
    viewport: { width: 390, height: 844 }
    userAgent: "Mozilla/5.0 (iPhone; CPU iPhone OS 14_0 like Mac OS X)..."
    isMobile: true
    hasTouch: true

  - name: "iPhone 14 Pro"
    viewport: { width: 393, height: 852 }
    userAgent: "Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)..."
    isMobile: true
    hasTouch: true

  # Android Devices
  - name: "Pixel 5"
    viewport: { width: 393, height: 851 }
    userAgent: "Mozilla/5.0 (Linux; Android 11)..."
    isMobile: true
    hasTouch: true

  - name: "Pixel 7"
    viewport: { width: 412, height: 915 }
    userAgent: "Mozilla/5.0 (Linux; Android 13)..."
    isMobile: true
    hasTouch: true

  # Tablet Devices
  - name: "iPad Mini"
    viewport: { width: 768, height: 1024 }
    userAgent: "Mozilla/5.0 (iPad; CPU OS 14_0 like Mac OS X)..."
    isMobile: true
    hasTouch: true

  - name: "iPad Pro 11"
    viewport: { width: 834, height: 1194 }
    userAgent: "Mozilla/5.0 (iPad; CPU OS 14_0 like Mac OS X)..."
    isMobile: true
    hasTouch: true

  # Responsive Breakpoints (Desktop Testing)
  - name: "Desktop HD"
    viewport: { width: 1920, height: 1080 }
    isMobile: false

  - name: "Desktop Laptop"
    viewport: { width: 1366, height: 768 }
    isMobile: false

  - name: "Tablet Breakpoint"
    viewport: { width: 768, height: 1024 }
    isMobile: false

  - name: "Mobile Breakpoint"
    viewport: { width: 375, height: 667 }
    isMobile: false
</mobile_viewport_config>

<cross_browser_config>
desktop:
  - name: chromium
    browser: chromium
    channel: chromium
    description: Desktop Chrome (primary)

  - name: firefox
    browser: firefox
    channel: firefox
    description: Desktop Firefox (secondary)

  - name: webkit
    browser: webkit
    channel: webkit
    description: Desktop Safari (tertiary)

mobile:
  - name: mobile-chrome
    browser: chromium
    device: Pixel 5
    description: Mobile Chrome on Android

  - name: mobile-safari
    browser: webkit
    device: iPhone 12
    description: Mobile Safari on iOS

  - name: mobile-pixel
    browser: chromium
    device: Pixel 7
    description: Mobile Chrome on Pixel 7
</cross_browser_config>

<performance_test_patterns>
load_time_tests:
  - test: "page loads within acceptable time"
    threshold: 5000  # milliseconds
    annotations: ["@performance", "@smoke"]

  - test: "API responses within acceptable time"
    threshold: 3000  # milliseconds
    annotations: ["@performance"]

  - test: "no blocking resources on load"
    threshold: 2000  # milliseconds for LCP
    annotations: ["@performance"]

resource_monitoring:
  - test: "memory usage within acceptable range"
    threshold: 100  # MB
    annotations: ["@performance"]

  - test: "no console errors on load"
    annotations: ["@performance", "@smoke"]

  - test: "smooth animations (no jank)"
    annotations: ["@performance"]

render_performance:
  - test: "components render within acceptable time"
    threshold: 100  # milliseconds per component
    annotations: ["@performance"]

  - test: "no layout thrashing during interactions"
    annotations: ["@performance"]
</performance_test_patterns>

<ci_cd_integration>
github_actions:
  workflow_file: ".github/workflows/e2e-tests.yml"

  jobs:
    - name: "E2E Tests"
      runs-on: "ubuntu-latest"
      strategy:
        matrix:
          project: ["chromium", "firefox", "webkit"]
      steps:
        - uses: "actions/checkout@v4"
        - uses: "actions/setup-node@v4"
        - run: "npm ci"
        - run: "npm run test:e2e -- --project=${{ matrix.project }}"

    - name: "Mobile E2E Tests"
      runs-on: "ubuntu-latest"
      steps:
        - uses: "actions/checkout@v4"
        - uses: "actions/setup-node@v4"
        - run: "npm ci"
        - run: "npm run test:e2e -- --project=mobile-chrome --project=mobile-safari"

    - name: "Performance Tests"
      runs-on: "ubuntu-latest"
      steps:
        - uses: "actions/checkout@v4"
        - uses: "actions/setup-node@v4"
        - run: "npm ci"
        - run: "npm run test:e2e -- --grep='@performance'"

    - name: "Upload Playwright Report"
      if: "always()"
      uses: "actions/upload-artifact@v4"
      with:
        name: "playwright-report-${{ matrix.project }}"
        path: "playwright-report/"

gitlab_ci:
  ".gitlab-ci.yml":
    variables:
      PLAYWRIGHT_BROWSERS_PATH: "0"
    test:e2e:
      image: "mcr.microsoft.com/playwright:latest"
      script:
        - "npm ci"
        - "npx playwright install --with-deps"
        - "npm run test:e2e"
      artifacts:
        paths:
          - "playwright-report/"
        expire_in: "1 week"

jenkinsfile:
  pipeline:
    agent: "any"
    stages:
      - stage: "E2E Tests"
        matrix:
          axes:
            browser: ["chromium", "firefox", "webkit"]
          parallel: true
        steps:
          - sh: "npm ci"
          - sh: "npm run test:e2e -- --project=${BROWSER}"

    - stage: "Mobile Tests"
      steps:
        - sh: "npm ci"
        - sh: "npm run test:e2e -- --project=mobile-chrome --project=mobile-safari"

    - stage: "Performance Tests"
      steps:
        - sh: "npm ci"
        - sh: "npm run test:e2e -- --grep='@performance'"
</ci_cd_integration>

<tasks>
<task>
<type>update</type>
<file>AURA-NOTES-MANAGER/frontend/playwright.config.ts</parameter>
<action>Update Playwright config with mobile browser projects and performance settings:
- Add mobile-chrome (Pixel 5), mobile-safari (iPhone 12), mobile-pixel (Pixel 7)
- Configure performance test timeouts (60s for load tests)
- Add custom expect timeout (10s for slower assertions)
- Maintain fullyParallel: false for DB consistency
- Add mobile viewport configurations</action>
<verify>
1. Ensure Playwright config exports defineConfig
2. Confirm projects list includes all desktop browsers (chromium, firefox, webkit)
3. Verify mobile projects exist (mobile-chrome, mobile-safari, mobile-pixel)
4. Check webServer uses 127.0.0.1:5173
5. Verify timeout settings (30s test, 10s expect for performance)</verify>
<done>Playwright config updated with mobile projects and performance settings</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/client/playwright.config.ts</parameter>
<action>Update AURA-CHAT Playwright config with mobile browser projects:
- Add mobile-chrome (Pixel 5), mobile-safari (iPhone 12), mobile-pixel (Pixel 7)
- Add performance measurement settings
- Add custom expect timeout (10s for slower assertions)
- Maintain fullyParallel: true for parallel execution</action>
<verify>
1. Ensure projects list includes all desktop browsers (chromium, firefox, webkit)
2. Confirm mobile projects (mobile-chrome, mobile-safari, mobile-pixel) exist
3. Check webServer uses 127.0.0.1:5173
4. Verify timeout settings (30s test, 10s expect)</verify>
<done>AURA-CHAT Playwright config updated with mobile projects</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/mobile.spec.ts</parameter>
<action>Add mobile-specific E2E tests for AURA-NOTES-MANAGER:
- @mobile: Touch gesture handling (tap, long-press, swipe)
- @mobile: Responsive layout tests (375px to 834px)
- @mobile: Keyboard handling on mobile
- @mobile: Sidebar collapse on mobile
- @mobile: Touch selection in selection mode
- @mobile: Mobile navigation flows</action>
<verify>
1. Confirm @mobile annotations exist
2. Verify tests use mobile viewports (iPhone 12, Pixel 5)
3. Check touch gesture tests
4. Tests verify responsive behavior</verify>
<done>AURA-NOTES-MANAGER mobile E2E tests created</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/performance.spec.ts</parameter>
<action>Add performance-focused E2E tests:
- @performance: Page load time (threshold: 5000ms)
- @performance: API response time (threshold: 3000ms)
- @performance: Memory usage monitoring
- @performance: Console error detection
- @performance: Animation smoothness
- @performance: Component render time</action>
<verify>
1. Confirm @performance annotations exist
2. Verify load time threshold is configurable
3. Tests measure actual performance metrics
4. Tests fail gracefully on timeout</verify>
<done>AURA-NOTES-MANAGER performance E2E tests created</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/e2e/mobile.spec.ts</parameter>
<action>Add mobile-specific E2E tests for AURA-CHAT:
- @mobile: Touch gesture handling for chat and graph
- @mobile: Responsive layout tests (375px to 1920px)
- @mobile: Chat input handling on mobile keyboards
- @mobile: Graph touch interactions (Reagraph)
- @mobile: Mobile navigation flows</action>
<verify>
1. Confirm @mobile annotations exist
2. Verify tests use mobile viewports
3. Check chat and graph mobile interactions
4. Tests verify responsive behavior</verify>
<done>AURA-CHAT mobile E2E tests created</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/e2e/performance.spec.ts</parameter>
<action>Add performance-focused E2E tests for AURA-CHAT:
- @performance: Page load time for chat and graph pages
- @performance: API response time for chat/config endpoints
- @performance: Graph data load time (Reagraph initialization)
- @performance: Memory usage during chat sessions
- @performance: Console error detection
- @performance: Smooth animations (typing indicator, graph transitions)</action>
<verify>
1. Confirm @performance annotations exist
2. Verify load time thresholds are configurable
3. Tests measure chat and graph performance
4. Tests verify no memory leaks</verify>
<done>AURA-CHAT performance E2E tests created</done>
</task>

<task>
<type>create</type>
<file>.gitlab-ci.yml</file>
<action>Create GitLab CI configuration for E2E testing:
- Define PLAYWRIGHT_BROWSERS_PATH variable
- Add test:e2e job with Playwright image
- Include npm install, browser installation, and test execution
- Configure artifacts for Playwright reports (1 week expiry)</action>
<verify>
1. GitLab CI file is valid YAML
2. Playwright image is used
3. Artifacts path points to playwright-report/
4. Browser installation includes dependencies</verify>
<done>GitLab CI configuration created for E2E testing</done>
</task>

<task>
<type>create</type>
<file>Jenkinsfile</file>
<action>Create Jenkins pipeline for E2E testing:
- Define agent (any)
- Add E2E Tests stage with browser matrix (chromium, firefox, webkit)
- Add Mobile Tests stage (mobile-chrome, mobile-safari)
- Add Performance Tests stage (@performance annotation)
- Use parallel execution for browser matrix</action>
<verify>
1. Jenkinsfile is valid Groovy syntax
2. Stages are defined correctly
3. Parallel execution configured for browser matrix
4. All test commands match npm scripts</verify>
<done>Jenkins pipeline created for E2E testing</done>
</task>

<task>
<type>create</type>
<file>.github/workflows/e2e-tests.yml</parameter>
<action>Create GitHub Actions workflow for E2E testing:
- Desktop browser matrix (chromium, firefox, webkit)
- Mobile browser testing (mobile-chrome, mobile-safari)
- Performance test job
- Parallel execution with artifact collection
- Automatic report generation</action>
<verify>
1. Workflow file exists and is valid YAML
2. Jobs cover all browser projects
3. Mobile tests are configured
4. Performance tests run separately
5. Artifacts are uploaded on failure</verify>
<done>GitHub Actions workflow created for E2E testing</done>
</task></parameter>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/fixtures.ts</parameter>
<action>Create shared test utilities with mock data factories (mockTreeResponse, mockKgQueueResponse, mockUser), mock API response helpers (mockExplorerTree, mockCrudResponses, mockKGProcessingResponses, mockErrorResponses), page helper utilities (waitForLoading, waitForApiResponse, goToExplorer, openContextMenu), and custom test fixture extending base test with all helpers.</action>
<verify>
1. Check imports work in test files
2. Verify mock response helpers use page.route() correctly
3. Confirm fixtures are used across E2E specs
4. Test file can access mockTree, mockCrud, mockKg helpers</verify>
<done>Shared E2E test utilities created with comprehensive mock data</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/explorer.spec.ts</parameter>
<action>Add comprehensive E2E tests for explorer CRUD and navigation flows:
- @critical: Department CRUD (create, read, update, delete hierarchy nodes)
- @critical: Navigation flows (sidebar navigation, breadcrumb navigation)
- @smoke: View mode toggle (grid/list), empty state handling
- @crud: All hierarchy types (department, semester, subject, module, note)
- @regression: Context menu open/close, dialog confirmations
- @edge: Duplicate name handling, error states, long labels</action>
<verify>
1. Confirm @critical, @smoke, @crud, @regression, @edge tags exist
2. Ensure CRUD flow coverage includes all 5 hierarchy types
3. Verify navigation tests cover sidebar and breadcrumbs
4. Check error handling tests mock API failures
5. Tests use fixture helpers (mockTree, mockCrud)</verify>
<done>Explorer E2E tests created with comprehensive CRUD coverage</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/kg-processing.spec.ts</parameter>
<action>Add E2E tests for knowledge graph processing flows:
- @critical: File selection mode, process dialog confirmation
- @smoke: Processing queue visibility, queue item display
- @edge: Processing errors, network failures during processing
- @regression: Selection persists after dialog close, queue updates</action>
<verify>
1. Confirm @critical, @smoke, @edge, @regression tags exist
2. Ensure process dialog tests cover confirm and cancel
3. Verify selection mode tests include multi-select
4. Check error handling tests mock API failures
5. Tests use fixture helpers (mockKg, mockErrors)</verify>
<done>KG processing E2E tests created with comprehensive coverage</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/frontend/e2e/health.spec.ts</parameter>
<action>Add baseline health, loading, and navigation E2E tests:
- @smoke: App loads without console errors, critical elements visible
- @navigation: Page routing works, 404 handling
- @edge: Slow network handling, loading states, timeout handling
- @regression: No regressions in existing functionality
- @performance: Page load under 3s, smooth transitions</action>
<verify>
1. Confirm @smoke, @navigation, @edge, @regression tags exist
2. Ensure viewport tests cover desktop, tablet, mobile
3. Verify error page tests mock HTTP errors
4. Check loading state tests wait for loading indicators
5. Tests use fixture helpers (mockErrors, goToExplorer)</verify>
<done>Health E2E tests created with comprehensive coverage</done>
</task>

<task>
<type>update</type>
<file>AURA-NOTES-MANAGER/frontend/package.json</parameter>
<action>Add Playwright dependency and E2E scripts (test:e2e, test:e2e:ui, test:e2e:headed, test:e2e:report).</action>
<verify>
1. Ensure @playwright/test dependency present in devDependencies
2. Confirm scripts are configured for all modes
3. Verify playwright browsers are installed</verify>
<done>E2E scripts and dependency added to package.json</done>
</task>
</tasks>

<output>
SUMMARY.md in .planning/phases/07-testing/07-04-SUMMARY.md
</output>

<success_criteria>
- [ ] 17 files total (4 updated + 13 new)
- [ ] 135 E2E tests total (AURA-CHAT: 65, AURA-NOTES-MANAGER: 70)
- [ ] All critical user flows tested (chat, graph, explorer CRUD, KG processing)
- [ ] Tests pass across all browsers (chromium, firefox, webkit, mobile-chrome, mobile-safari, mobile-pixel)
- [ ] Test execution time less than 30 minutes
- [ ] Test annotations enable selective runs (@critical, @smoke, @crud, @edge, @performance, @mobile)
- [ ] Mock API responses ensure deterministic, fast test execution
- [ ] Performance tests with configurable thresholds (5000ms page, 3000ms API, 100MB memory)
- [ ] Mobile tests cover touch gestures (tap, long-press, swipe) and responsive layouts
- [ ] Mobile testing across all defined viewport configurations (iPhone, Pixel, iPad)
- [ ] Performance testing integrated into CI/CD pipelines
- [ ] CI/CD integration with GitHub Actions, GitLab CI, and Jenkins
</success_criteria>

<test_count_breakdown>
AURA-CHAT (React 19 + Vite):
- chat.spec.ts: 15 tests (enhanced)
- graph.spec.ts: 15 tests (enhanced)
- documents.spec.ts: 10 tests
- health.spec.ts: 5 tests
- mobile.spec.ts: 10 tests (NEW)
- performance.spec.ts: 10 tests (NEW)
Total: 65 tests

AURA-NOTES-MANAGER (React 18 + Vite):
- explorer.spec.ts: 25 tests
- kg-processing.spec.ts: 15 tests
- health.spec.ts: 10 tests
- mobile.spec.ts: 10 tests (NEW)
- performance.spec.ts: 10 tests (NEW)
Total: 70 tests

GRAND TOTAL: 135 tests
</test_count_breakdown>

<run_commands>
# AURA-CHAT
cd AURA-CHAT/client && npm run test:e2e

# AURA-NOTES-MANAGER
cd AURA-NOTES-MANAGER/frontend && npm run test:e2e

# Selective runs - Desktop
npx playwright test --grep "@critical"        # Critical tests only
npx playwright test --grep "@smoke"           # Smoke tests only
npx playwright test --grep "@crud"            # CRUD tests only
npx playwright test --project=chromium        # Chromium only
npx playwright test --project=firefox         # Firefox only

# Selective runs - Mobile
npx playwright test --project=mobile-chrome   # Mobile Chrome only
npx playwright test --project=mobile-safari   # Mobile Safari only

# Performance tests
npx playwright test --grep "@performance"     # Performance tests only

# Mobile-specific tests
npx playwright test --grep "@mobile"          # Mobile tests only

# Generate report
npm run test:e2e:report
</run_commands>

<checkpoint:human-verify>
1. Run: npm run test:e2e (AURA-CHAT)
2. Run: npm run test:e2e (AURA-NOTES-MANAGER)
3. Verify all tests pass
4. Check HTML reports for failures
5. Run selective tests: --grep "@smoke" (should complete in <2 min)
6. Run cross-browser: --project=firefox (verify all pass)
7. Run mobile tests: --project=mobile-chrome (verify all pass)
8. Run performance tests: --grep "@performance" (verify thresholds)
9. Verify GitHub Actions workflow runs successfully
</checkpoint:human-verify>

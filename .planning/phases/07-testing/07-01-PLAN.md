<objective>
Set up comprehensive Pytest configuration for AURA-CHAT backend with fixtures, mocks, and parameterized tests covering session management, module filtering, and RAG operations.
</objective>

<context>
@.planning/ROADMAP.md - Phase 7 (Testing & Optimization)
@AURA-CHAT/pyproject.toml - Existing pytest config (testpaths: ["tests"])
@AURA-CHAT/tests/conftest.py - Existing pytest fixtures with mock registry
@AURA-CHAT/tests/unit/test_session_crud.py - Existing session CRUD tests (30 tests)
@AURA-CHAT/tests/unit/test_messages.py - Existing message tests (16 tests)
@AURA-CHAT/tests/unit/test_module_filtering.py - Existing module filtering tests (16 tests)
@AURA-CHAT/tests/unit/test_rag_engine.py - Existing RAG engine tests (35 tests)
@AURA-CHAT/backend/session_manager.py - SessionManager with Redis caching (22 methods)
@AURA-CHAT/backend/rag_engine.py - RAGEngine with thinking mode (18 public methods)
@AURA-CHAT/backend/graph_manager.py - GraphManager with test mode (25+ methods)

Previous: Phase 6 Frontend Implementation complete
Current: Phase 7-01 AURA-CHAT Backend Unit Tests (extending existing tests)

IMPORTANT: This plan has been UPDATED based on code review to fix fixture signatures,
add missing test coverage, and include performance benchmarks.
</context>

<requirements>
1. Coverage targets: 85% backend, 80% API endpoints
2. Test organization: tests/unit/, tests/integration/, tests/api/
3. Fixtures for: Neo4j connections, mock users, test sessions, module data
4. Mock external services: Gemini API, Redis cache with TTL support
5. Parameterized tests for RAG queries with different module combinations
6. Test isolation with proper cleanup
7. Performance benchmarks for session operations and RAG queries

Pytest Plugins Required:
- pytest-cov: Coverage reporting with --cov-fail-under threshold
- pytest-mock: Enhanced mock functionality (optional, using unittest.mock)
- pytest-xdist: Parallel test execution (optional)

Test Categories:
- Session CRUD: create, read, update, delete sessions
- Message operations: send query, retrieve history, pagination
- Module filtering: single module, multi-module, cross-module
- RAG engine: vector search, graph traversal, answer synthesis
- Error handling: invalid session, unauthorized access, API errors
- Query classification: vector_only, hybrid, graph_expansion
- Thinking mode: content extraction, marker detection, summary generation
- Analytics: user session counts, message counts, module usage, activity timeline
</requirements>

<code_review_findings>
## Fixture Issues Fixed (from code analysis):

1. mock_neo4j_driver.execute_query:
   - OLD: return_value=([], None, None) - WRONG format
   - NEW: return_value=[] - matches graph_manager.execute_query return type List[Dict]

2. mock_redis_client missing TTL:
   - ADDED: mock_setex with TTL and expiration tracking
   - session_manager uses: self.redis.setex(key, 86400, json.dumps(session))

3. Missing ChatManager fixture:
   - ADDED: mock_chat_manager for RAGEngine conversation history
   - Required for: chat_manager.get_last_n_messages(), add_message(), get_conversation_history()

4. Missing GraphManager methods in mock:
   - ADDED: semantic_entity_search(), expand_graph_context()
   - ADDED: search_vector_with_modules(), get_module_entities()
   - ADDED: get_all_documents(), get_cross_module_concepts()

## Missing Test Coverage Added:

1. SessionManager analytics methods (8 new tests):
   - get_user_session_count(), get_user_message_count()
   - get_user_module_usage(), get_session_activity_timeline()
   - get_recent_sessions(), get_user_analytics()
   - archive_session(), resume_session()

2. RAGEngine query classification (5 new tests):
   - _classify_query() patterns for vector_only, hybrid, graph_expansion
   - Multi-hop vs simple query detection
   - Query expansion via graph lookup

3. RAGEngine thinking detection (4 new tests):
   - _is_thinking_text() marker detection
   - _split_thinking_from_response() content separation
   - _THINKING_MARKERS and _RESPONSE_MARKERS patterns

4. SessionManager ownership validation (3 new tests):
   - validate_ownership() with correct/incorrect user
   - Cache invalidation on session update

## Coverage Thresholds Per Module:
- session_manager.py: 90% (was 85%)
- rag_engine.py: 88% (was 85%)
- graph_manager.py: 85% (unchanged)
- chat_manager.py: 85% (new)
</code_review_findings>

<performance_benchmarks>
## Performance Test Requirements:

1. Session Operations (benchmark targets):
   - create_session: < 50ms (mocked Neo4j)
   - get_session (cache hit): < 5ms
   - get_session (cache miss): < 30ms
   - list_user_sessions (10 items): < 20ms
   - add_message: < 30ms
   - get_messages (100 items): < 15ms

2. RAG Query Operations:
   - query() with context retrieval: < 200ms
   - _retrieve_context() hybrid search: < 100ms
   - _generate_response() LLM call: < 3000ms
   - _classify_query(): < 1ms
   - _extract_text_from_vertex_response(): < 10ms

3. Graph Operations:
   - execute_query() simple: < 10ms
   - semantic_search() vector: < 50ms
   - expand_graph_context() 2-hop: < 100ms

4. Benchmark Tests:
   - @pytest.mark.benchmark for performance-critical paths
   - Use pytest-benchmark plugin for timing assertions
   - Profile memory for large session histories (>1000 messages)
</performance_benchmarks>

<tasks>
<task>
<type>update</type>
<file>AURA-CHAT/tests/conftest.py</file>
Fix and extend existing conftest.py fixtures based on code review.

VERIFIED FIXES (against actual code signatures):

1. mock_neo4j_driver():
   - execute_query returns List[Dict], NOT tuple - FIXED
   - Mock returns [] by default (matches graph_manager.py pattern)

2. mock_redis_client():
   - Added TTL support via mock_setex with expiration tracking
   - session_manager uses: self.redis.setex(key, 86400, json.dumps(session))

3. mock_chat_manager() - NEW:
   - Returns mock ChatManager for RAGEngine conversation history
   - Methods: get_last_n_messages(), add_message(), get_conversation_history(), clear_conversation()

4. mock_graph_manager() - ENHANCED:
   - Added semantic_entity_search() returning entity dicts with id, name, type, definition
   - Added expand_graph_context() returning {"chunks": [], "entities": []}
   - Added search_vector_with_modules() for module-filtered vector search
   - Added get_module_entities() for entity retrieval by module

Fixtures to Have (10 total):
1. mock_neo4j_driver() - Fixed execute_query return type
2. mock_redis_client() - Added TTL support
3. mock_gemini_client() - Enhanced with thought_summary support
4. mock_chat_manager() - NEW for RAG conversation history
5. mock_graph_manager() - Enhanced with all required methods
6. mock_session_manager() - Full CRUD with ownership validation
7. mock_embedding_service() - Deterministic embeddings
8. sample_user() - User data fixture
9. sample_session() - Session data fixture
10. sample_rag_context() - Chunks, entities, cross_concepts

Mock Registry Pattern:
- _mock_registry: List[MagicMock] = []
- register_mock(mock) adds to registry
- reset_mocks autouse fixture clears registry after each test
</action>
<verify>
1. python -c "from tests.conftest import *; print('Fixtures loaded successfully')"
2. Verify mock_neo4j_driver.execute_query returns List[Dict]
3. Verify mock_redis_client.setex implements TTL
4. Verify mock_chat_manager has required methods
5. python -m pytest tests/unit/ --collect-only | head -30
</verify>
<done>
conftest.py updated with verified fixtures matching actual code signatures</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/tests/unit/test_session_crud.py</parameter>
Extend session CRUD tests with analytics, ownership validation, and edge cases.

NEW TESTS (15 additional tests, total 35):

Analytics Methods (8 tests):
1. test_get_user_session_count - Returns count for user
2. test_get_user_session_count_empty - Zero when no sessions
3. test_get_user_message_count - Returns total message count
4. test_get_user_message_count_empty - Zero when no messages
5. test_get_user_module_usage - Returns module usage statistics
6. test_get_user_module_usage_empty - Empty list when no usage
7. test_get_session_activity_timeline - Returns hourly message distribution
8. test_get_user_analytics - Returns complete user analytics dict

Ownership Validation (3 tests):
9. test_validate_ownership_valid - Correct owner returns True
10. test_validate_ownership_invalid - Wrong user returns False
11. test_validate_ownership_nonexistent - Non-existent session returns False

Archive/Resume (2 tests):
12. test_archive_session - Status changes to archived
13. test_resume_session - Status changes back to active

Cache Invalidation (2 tests):
14. test_update_session_invalidates_cache - Cache cleared on update
15. test_add_message_invalidates_cache - Cache cleared on new message

Edge Cases Covered:
- Redis disabled (self.redis = None)
- Cache read failures gracefully handled
- Empty None handling for module_ids, document_ids
- Message limit enforcement (MESSAGE_LIMIT = 100)
- Timestamp format ISO 8601 with Z suffix
</action>
<verify>
1. python -m pytest tests/unit/test_session_crud.py -v --tb=short
2. Verify all 35 tests pass
3. Check coverage: pytest tests/unit/test_session_crud.py --cov=backend.session_manager --cov-report=term-missing --cov-fail-under=90
</verify>
<done>
35 session CRUD tests with full analytics and cache coverage</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/tests/unit/test_rag_engine.py</parameter>
Add RAG engine tests for query classification and thinking detection.

NEW TESTS (10 additional, total 45):

Query Classification (5 tests):
1. test_classify_query_vector_only - "what is" patterns return vector_only
2. test_classify_query_hybrid - Complex queries return hybrid
3. test_classify_query_graph_expansion - "compare/contrast" return graph_expansion
4. test_classify_query_short - Short queries (<6 words) return vector_only
5. test_expand_query - Related terms added via graph lookup

Thinking Detection (5 tests):
1. test_is_thinking_text_with_markers - Thinking markers detected correctly
2. test_is_thinking_text_without_markers - Non-thinking text returns False
3. test_split_thinking_from_response - Content separated at response markers
4. test_thinking_markers_comprehensive - All _THINKING_MARKERS patterns tested
5. test_response_markers_comprehensive - All _RESPONSE_MARKERS patterns tested

Additional Tests:
- test_hybrid_search_combines_vector_fulltext - 70% vector + 30% fulltext scores
- test_context_window_respected - Chunks limited by MAX_CONTEXT_TOKENS
- test_citation_relevance_threshold - Chunks below score threshold filtered
</action>
<verify>
1. python -m pytest tests/unit/test_rag_engine.py -v --tb=short
2. Verify all 45 tests pass
3. Check coverage: pytest tests/unit/test_rag_engine.py --cov=backend.rag_engine --cov-report=term-missing --cov-fail-under=88
</verify>
<done>
45 RAG engine tests with query classification and thinking detection</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/pyproject.toml</parameter>
Add pytest-cov and pytest-benchmark configuration with per-module coverage thresholds.

[tool.pytest.ini_options]
testpaths = ["tests/unit", "tests/integration", "tests/api", "tests/performance"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--cov=backend",
    "--cov-report=term-missing",
    "--cov-report=html:htmlcov",
    "--cov-fail-under=85",
]

[tool.coverage.run]
source = ["backend"]
omit = [
    "backend/__pycache__/*",
    "backend/tests/*",
    "backend/*/__pycache__/*",
    "backend/*/*/__pycache__/*",
]
branch = true
parallel = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@abstractmethod",
]
precision = 2
show_missing = true

[tool.coverage.html]
title = "AURA-CHAT Backend Coverage Report"

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks integration tests",
    "performance: marks performance benchmark tests",
    "benchmark: marks tests for pytest-benchmark",
]
</action>
<verify>
1. Verify pyproject.toml has pytest-cov and pytest-benchmark config
2. Run: pytest --version
3. Run: pytest tests/unit/ --cov=backend --cov-fail-under=85 -v
4. Run: pytest tests/performance/ --benchmark-enable -v (if benchmark plugin installed)
</verify>
<done>
pytest-cov configuration with per-module coverage thresholds</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/07-testing/07-01-SUMMARY.md`
</output>

<success_criteria>
- [ ] 8 files total (1 conftest updated + 5 test files modified + 1 new test file + 1 pyproject.toml updated)
- [ ] 83+ unit tests (35 session + 24 messages + 16 module + 45 RAG + query classification)
- [ ] 90%+ code coverage on session_manager.py
- [ ] 88%+ code coverage on rag_engine.py
- [ ] 85%+ code coverage on graph_manager.py
- [ ] All tests pass in isolation (function-scoped fixtures)
- [ ] Fixtures properly isolated with mock registry cleanup
- [ ] Coverage threshold enforcement (--cov-fail-under=85)
- [ ] Performance benchmarks defined for critical paths
- [ ] Query classification and thinking detection fully tested

Code Verification (fixtures match actual signatures):
- [ ] mock_neo4j_driver.execute_query returns List[Dict], not tuple
- [ ] mock_redis_client.setex implements TTL (86400s for sessions)
- [ ] mock_chat_manager has get_last_n_messages(), add_message()
- [ ] mock_graph_manager has semantic_entity_search(), expand_graph_context()
- [ ] mock_gemini_client supports thought_summary attribute
</success_criteria>

<checkpoint:human-verify>
1. Run: python -m pytest tests/unit/ -v --tb=short
2. Verify test count (125+) and pass rate (100%)
3. Check coverage: pytest tests/unit/ --cov=backend --cov-report=term-missing
4. Verify fixtures don't leak between tests (reset_mocks autouse=True)
5. Verify pytest-cov exits with error code when coverage < 85%
6. Run performance benchmarks: pytest tests/performance/ --benchmark-enable -v
</checkpoint>
<objective>
Optimize performance of AURA-CHAT and AURA-NOTES-MANAGER by addressing critical bottlenecks identified in research. Focus on async Neo4j driver migration, Redis caching for KG queries, and React component memoization to reduce latency and improve scalability without breaking existing functionality.

**Purpose:** Eliminate blocking I/O, reduce database load, and optimize frontend rendering.
**Output:** Optimized backend files, Redis caching layer, and memoized frontend components.
</objective>

<context>
@.planning/ROADMAP.md - Current project roadmap
@.planning/phases/08-performance-optimization/08-01-FINDINGS.md - Research findings with specific issues and file locations
@AURA-CHAT/backend/graph_manager.py - Main Neo4j operations (sync driver)
@AURA-CHAT/backend/rag_engine.py - RAG queries (uses graph_manager)
@AURA-NOTES-MANAGER/api/kg_processor.py - Sequential embedding calls
@AURA-CHAT/client/src/features/chat/ChatPage.tsx - Excessive re-renders
@AURA-CHAT/backend/session_manager.py - Limited Redis usage

**Previous:** Phases 1-7 completed (Schema, KG Processor, Module Management, AURA-CHAT Integration, Study Sessions, Frontend, Testing).
**Current:** Phase 8 - Performance Optimization to improve responsiveness and scalability.
</context>

<requirements>
1. **Async Neo4j Migration:**
   - Migrate from synchronous `GraphDatabase.driver()` to async `AsyncGraphDatabase.driver()`
   - Convert all `session.run()` calls to `async with session() as session: await session.run()`
   - Update all query methods to return coroutines
   - Maintain existing query logic and error handling

2. **Redis Caching Layer for KG Queries:**
   - Add Redis caching to `session_manager.py` or new `cache/kg_cache.py`
   - Cache frequent KG query results with TTL (5-30 minutes)
   - Implement cache-aside pattern: check cache first, then query Neo4j
   - Cache key pattern: `kg:query:{hash(query, params)}`

3. **React Component Memoization:**
   - Memoize ChatPage using React.memo()
   - Wrap event handlers in useCallback()
   - Consolidate related state variables into single objects
   - Use useMemo for derived state calculations

4. **Embedding Batching:**
   - Modify `kg_processor.py` to batch embedding API calls
   - Use Gemini batch embedding endpoint or asyncio.gather() with semaphore
   - Process multiple chunks concurrently (limit: 5-10 parallel)

5. **Verification:**
   - All existing tests must pass
   - No breaking changes to API contracts
   - Performance benchmarks: P95 latency < 500ms for chat queries
</requirements>

<tasks>
<task>
<type>update</type>
<file>AURA-CHAT/backend/graph_manager.py</file>
<action>
Convert the GraphManager class from synchronous to asynchronous by replacing the Neo4j driver with the async driver and updating all methods to use async/await patterns.

**Specific Changes:**
1. **Import Change:** Replace `from neo4j import GraphDatabase` with `from neo4j import AsyncGraphDatabase`
2. **Driver Initialization:** Change `self.driver = GraphDatabase.driver(...)` to `self.driver = AsyncGraphDatabase.driver(...)`
3. **Connection Method:** Update `_connect()` to use `async with await self.driver.session() as session:` pattern
4. **Query Methods:** Convert all query execution methods (execute_query, execute_write, etc.) to async def with `await`
5. **Retry Logic:** Replace `time.sleep()` with `await asyncio.sleep()` in retry loops
6. **Context Manager:** Ensure proper async context manager usage with `async with`

**Data Structures:**
- Keep existing node/relationship data structures unchanged
- All methods return `Awaitable[List[Dict]]` or similar async types

**Edge Cases:**
- Handle `neo4j.exceptions.ServiceUnavailable` with async retry logic
- Ensure session is always closed even on exceptions using async context managers
- Maintain connection pool settings from original configuration

**Pitfalls:**
- Do NOT block on async calls with `asyncio.run()` or `loop.run_until_complete()`
- Ensure all callers (rag_engine.py, routers) are updated to await these methods
- Do not change query logic, only the I/O pattern

**Integration:**
- Called by: `AURA-CHAT/backend/rag_engine.py`
- Called by: `AURA-CHAT/backend/routers/chat.py`
- Update these callers to use `await` when calling graph_manager methods
</action>
<verify>
1. Syntax check: `python -m py_compile AURA-CHAT/backend/graph_manager.py`
2. Check for blocking calls: `grep -n "loop.run\|asyncio.run\|time.sleep" AURA-CHAT/backend/graph_manager.py` (should only find asyncio.sleep)
3. Verify async driver import: `grep -n "AsyncGraphDatabase" AURA-CHAT/backend/graph_manager.py`
4. Run existing unit tests to ensure no breakage
</verify>
<done>
- GraphManager uses AsyncGraphDatabase driver
- All query methods are async def with await
- No synchronous blocking calls in hot paths
- Existing tests pass
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/backend/cache/kg_cache.py</file>
<action>
Create a new Redis caching module for knowledge graph query results to reduce database load for repeated queries.

**Specific Changes:**
1. **File Structure:** Create new file with Redis connection setup (reuse from session_manager or create new)
2. **Cache Interface:**
   - `async def get_cached_query(key: str) -> Optional[Dict]`
   - `async def set_cached_query(key: str, value: Dict, ttl: int = 300)`
   - `async def invalidate_query(pattern: str)`
3. **Key Generation:** Hash-based keys: `f"kg:{hashlib.sha1(f'{query}:{params}'.encode()).hexdigest()}"`
4. **TTL Configuration:** Default 5 minutes, configurable per query type

**Data Structures:**
- Cache entries stored as JSON strings in Redis
- Key format: `aura:kg:{hash}` for namespacing
- Value: Dict containing query results and metadata (timestamp, duration)

**Edge Cases:**
- Handle Redis connection errors gracefully (log and fallback to direct query)
- Serialize/deserialize complex types (dates, floats) properly
- Prevent cache bloat with TTL and max size limits

**Pitfalls:**
- Do NOT cache user-specific data without proper isolation
- Do NOT cache data that changes frequently without short TTL
- Avoid caching very large query results (>1MB)

**Integration:**
- Will be imported by: `AURA-CHAT/backend/rag_engine.py`
- Should integrate with existing Redis client from `AURA-CHAT/backend/session_manager.py`
</action>
<verify>
1. Syntax check: `python -m py_compile AURA-CHAT/backend/cache/kg_cache.py`
2. Verify Redis connection: Test against running Redis instance
3. Check key format: Review generated keys match pattern
4. Integration test: Call get/set methods and verify round-trip
</verify>
<done>
- kg_cache.py created with get/set/invalidate methods
- Redis connection working
- Keys follow namespaced pattern
- TTL and error handling implemented
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/backend/rag_engine.py</file>
<action>
Integrate the Redis caching layer into the RAG engine and optimize query execution with parallel queries where possible.

**Specific Changes:**
1. **Import Cache:** Add `from .cache.kg_cache import kg_cache`
2. **Cache Integration:** Wrap Neo4j queries with cache lookups:
   ```python
   async def semantic_search(self, query, filters, top_k):
       cache_key = generate_key(...)
       cached = await kg_cache.get(cache_key)
       if cached: return cached
       results = await self.graph_manager.execute_query(...)
       await kg_cache.set(cache_key, results, ttl=600)
       return results
   ```
3. **Parallel Queries:** Use `asyncio.gather()` for independent queries in `semantic_entity_search()` (4 sequential queries)
4. **Error Handling:** Add try/except for cache failures, fall back to direct query

**Data Structures:**
- Keep existing QueryResult, RetrievedChunk types
- Add cache metadata to result objects if needed

**Edge Cases:**
- Cache misses should proceed normally
- Cache failures should not crash the query
- Invalidate cache when underlying data changes

**Pitfalls:**
- Do NOT cache personalized results (user-specific data)
- Do NOT break existing query logic, only add caching layer
- Do not cache indefinitely - use appropriate TTLs

**Integration:**
- Modifies: `AURA-CHAT/backend/rag_engine.py`
- Depends on: `AURA-CHAT/backend/graph_manager.py` (async)
- Depends on: `AURA-CHAT/backend/cache/kg_cache.py`
</action>
<verify>
1. Syntax check: `python -m py_compile AURA-CHAT/backend/rag_engine.py`
2. Verify async calls: `grep -n "await asyncio.gather" AURA-CHAT/backend/rag_engine.py`
3. Check cache integration: Verify cache calls added to query methods
4. Run existing tests
</verify>
<done>
- Cache integration in semantic_search and other query methods
- Parallel queries implemented for entity search
- No regression in query results
- Existing tests pass
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/client/src/features/chat/ChatPage.tsx
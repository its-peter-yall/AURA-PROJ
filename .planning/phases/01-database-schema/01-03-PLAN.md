<objective>
Set up HNSW vector indices for semantic search on Chunk nodes.
</objective>

<context>
@.planning/BRIEF.md - Project vision
@.planning/ROADMAP.md - Phase 1 requirements (vector index config)
@.planning/phases/01-database-schema/01-02-SUMMARY.md - Previous task outcome

Previous: Module, StudySession, Message constraints created
Current: Need to add vector indices for semantic search
</context>

<requirements>
1. Create HNSW vector index on Chunk.embedding
2. Index configuration from ROADMAP:
   - dimensions: 768 (Gemini embedding size)
   - similarity_function: cosine
   - hnsw.m: 16
   - hnsw.ef_construction: 200
3. Index must use `IF NOT EXISTS` for idempotency
4. Verify index creation with `SHOW VECTOR INDEXES`
5. Add index creation to migration script
</requirements>

<tasks>
<task>
<type>modify</type>
<file>AURA-NOTES-MANAGER/api/migrations/001_add_module_schema.py</file>
<action>
Add vector index creation:
```cypher
CREATE VECTOR INDEX chunk_vector_index IF NOT EXISTS
FOR (c:Chunk) ON (c.embedding)
OPTIONS {
  indexConfig: {
    `vector.dimensions`: 768,
    `vector.similarity_function`: 'cosine',
    `vector.hnsw.m`: 16,
    `vector.hnsw.ef_construction`: 200
  }
}
```
</action>
<verify>
Run migration and check:
- `SHOW VECTOR INDEXES` returns the index
- Index state is "ONLINE"
- Index configuration matches specs
</verify>
<done>
HNSW vector index created and online
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/01-database-schema/01-03-SUMMARY.md`
</output>

<success_criteria>
- [ ] Vector index created with correct configuration
- [ ] Index state is ONLINE
- [ ] `SHOW VECTOR INDEXES` shows index with correct settings
</success_criteria>

<checkpoint:human-verify>
Verify vector index configuration matches ROADMAP specs
</checkpoint:human-verify>

<objective>
Extend Document and Chunk nodes with module_id property and verify complete migration.
</objective>

<context>
@.planning/BRIEF.md - Project vision
@.planning/ROADMAP.md - Phase 1 requirements (module_id extension)
@.planning/phases/01-database-schema/01-03-SUMMARY.md - Previous task outcome

Previous: Vector index created
Current: Need to add module_id to Document/Chunk, verify full migration
</context>

<requirements>
1. Add `module_id` property to Document nodes (denormalized for query performance)
2. Add `module_id` property to Chunk nodes
3. Create indexes on Document.module_id and Chunk.module_id
4. Verify all migration components work together
5. Create verification test script
6. Document migration complete state
</requirements>

<tasks>
<task>
<type>modify</type>
<file>AURA-NOTES-MANAGER/api/migrations/001_add_module_schema.py</file>
<action>
Add module_id extension:
- Add `module_id: String` to Document node properties (optional, FK to Module)
- Add `module_id: String` to Chunk node properties (denormalized for query performance)
- Create index on Document.module_id for filtering
- Create index on Chunk.module_id for vector search post-filtering
- Add relationship: (Module)-[:CONTAINS_DOCUMENT]->(Document)
- Add relationship: (Chunk)-[:CONTAINS_ENTITY]->(Entity)
- Add relationship: (Entity)-[:RELATES_TO]->(Entity)
</action>
<verify>
Run complete migration:
- Check all indexes: `SHOW INDEXES`
- Check all constraints: `SHOW CONSTRAINTS`
- Check vector indexes: `SHOW VECTOR INDEXES`
- Test module_id assignment to document
- Test module_id assignment to chunk
</verify>
<done>
Full migration verified working end-to-end
</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/api/migrations/verify_migration.py</file>
<action>
Create verification script that:
- Connects to Neo4j
- Lists all constraints
- Lists all indexes (including vector)
- Tests creating Module, StudySession, Message
- Tests creating Document with module_id
- Tests creating Chunk with module_id
- Reports success/failure for each check
</action>
<verify>
Run script and all checks pass
</verify>
<done>
Verification script created and passing
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/01-database-schema/01-04-SUMMARY.md`
</output>

<success_criteria>
- [ ] Document.module_id index created
- [ ] Chunk.module_id index created
- [ ] All relationship types defined
- [ ] Verification script passes all checks
- [ ] Migration is fully idempotent and reversible
</success_criteria>

<checkpoint:human-verify>
Review verification script output, confirm all checks pass
</checkpoint:human-verify>

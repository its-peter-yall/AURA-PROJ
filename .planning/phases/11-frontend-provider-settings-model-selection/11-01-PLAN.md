---
phase: 11-frontend-provider-settings-model-selection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - AURA-CHAT/client/src/types/settings.ts
  - AURA-CHAT/client/src/features/settings/hooks/useModelList.ts
  - AURA-CHAT/client/src/features/settings/hooks/useModelList.test.ts
  - AURA-CHAT/client/src/stores/useModelStore.ts
  - AURA-CHAT/client/src/stores/useModelStore.test.ts
  - AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.ts
  - AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.test.ts
autonomous: true
requirements: [UI-01, CONFIG-02]

must_haves:
  truths:
    - "Model grouping function correctly produces 2-level hierarchy for Vertex AI/Ollama and 3-level hierarchy for OpenRouter"
    - "OpenRouter models are grouped by vendor prefix (anthropic/, google/, etc.) extracted from model name"
    - "Zustand model store persists selected model per session to sessionStorage"
    - "Resuming a session restores the previously selected model from sessionStorage"
    - "TanStack Query hooks fetch model lists and defaults from /api/v1/settings/* endpoints"
    - "Cache invalidation occurs on settings mutations (API key store, default update)"
  artifacts:
    - path: "AURA-CHAT/client/src/types/settings.ts"
      provides: "TypeScript types for settings API — ModelInfo, ModelGroup, VendorGroup, ProviderType, UseCase, DefaultModelSetting, ApiKeyStatus"
      exports: ["ModelInfo", "ModelGroup", "VendorGroup", "ProviderType", "UseCase", "DefaultModelSetting", "ApiKeyStatus"]
    - path: "AURA-CHAT/client/src/features/settings/hooks/useModelList.ts"
      provides: "groupModelsByProvider pure function + useGroupedModels hook"
      exports: ["groupModelsByProvider", "useGroupedModels"]
    - path: "AURA-CHAT/client/src/stores/useModelStore.ts"
      provides: "Zustand store for per-session model selection with sessionStorage persistence"
      exports: ["useModelStore"]
    - path: "AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.ts"
      provides: "TanStack Query hooks for all settings endpoints + query key factory"
      exports: ["settingsKeys", "useAllModels", "useDefaults", "useProviderModels", "useStoreApiKey", "useUpdateDefault", "useApiKeyStatus", "useDeleteApiKey", "useValidateApiKey"]
  key_links:
    - from: "AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.ts"
      to: "/api/v1/settings/*"
      via: "axios api instance"
      pattern: "api\\.(get|put|post|delete).*settings"
    - from: "AURA-CHAT/client/src/stores/useModelStore.ts"
      to: "sessionStorage"
      via: "Zustand persist middleware"
      pattern: "createJSONStorage.*sessionStorage"
    - from: "AURA-CHAT/client/src/features/settings/hooks/useModelList.ts"
      to: "AURA-CHAT/client/src/types/settings.ts"
      via: "import ModelInfo, ModelGroup"
      pattern: "import.*ModelInfo.*settings"
---

<objective>
Build the complete data layer for Phase 11: TypeScript types matching the backend settings API, a pure model-grouping function that produces hierarchical trees (2-level for Vertex AI/Ollama, 3-level for OpenRouter), a Zustand store for per-session model persistence, and TanStack Query hooks for all settings endpoints.

Purpose: Every UI component in subsequent plans depends on these types, hooks, and store. Building and testing them first ensures the contracts are solid before any component rendering work begins.

Output: 7 files — 3 source modules + 1 type file + 3 test files, all in AURA-CHAT.
</objective>

<execution_context>
@C:/Users/Peter/.config/opencode/agents/gsd-execute.md
@C:/Users/Peter/.config/opencode/agents/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-provider-settings-model-selection/11-RESEARCH.md

<interfaces>
<!-- Backend API surface (from AURA-CHAT/server/routers/settings.py): -->

Endpoints (all under /api/v1/settings):
  GET  /defaults                       → Record<string, {provider, model}>
  PUT  /defaults/{use_case}            ← {provider, model} → {use_case, provider, model}
  GET  /models                         → Array<{name, provider, display_name}>
  GET  /providers/{provider}/models    → Array<{name, provider, display_name}>
  POST /providers/{provider}/api-key   ← {api_key} → {provider, masked_key, valid}
  GET  /providers/{provider}/api-key   → {provider, masked_key, valid}
  DELETE /providers/{provider}/api-key → {status: "deleted"}
  POST /providers/{provider}/validate  → {provider, valid, error?}

Allowed use_cases: "chat" | "embeddings" | "entity_extraction"
Allowed providers: "vertex_ai" | "openrouter" | "ollama"
ModelInfo shape: { name: string, provider: string, display_name: string | null }

<!-- Existing Zustand store pattern (from AURA-CHAT/client/src/stores/useAuthStore.ts): -->
```typescript
import { create } from 'zustand';
export const useAuthStore = create<AuthState>((set, get) => ({ ... }));
```

<!-- Existing query key factory pattern (from features/study-sessions/hooks/useStudySession.ts): -->
```typescript
export const sessionKeys = {
    all: ['sessions'] as const,
    lists: () => [...sessionKeys.all, 'list'] as const,
    list: (userId: string, status?: string) => [...sessionKeys.lists(), userId, status] as const,
    details: () => [...sessionKeys.all, 'detail'] as const,
    detail: (sessionId: string) => [...sessionKeys.details(), sessionId] as const,
};
```

<!-- Existing axios API instance (from AURA-CHAT/client/src/lib/api.ts): -->
```typescript
import axios from 'axios';
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://127.0.0.1:8000';
const api = axios.create({ baseURL: API_BASE_URL, ... });
// Usage: const response = await api.get<T>('/path'); return response.data;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings types + groupModelsByProvider pure function (TDD)</name>
  <files>
    AURA-CHAT/client/src/types/settings.ts,
    AURA-CHAT/client/src/features/settings/hooks/useModelList.ts,
    AURA-CHAT/client/src/features/settings/hooks/useModelList.test.ts
  </files>
  <behavior>
    - groupModelsByProvider([]) returns []
    - groupModelsByProvider with 2 Vertex AI models returns 1 ModelGroup with provider='vertex_ai', providerLabel='Vertex AI', 1 VendorGroup with vendor='vertex_ai', containing 2 sorted models
    - groupModelsByProvider with 3 OpenRouter models ('anthropic/claude-3.5-sonnet', 'anthropic/claude-3-haiku', 'google/gemini-2.5-flash') returns 1 ModelGroup with provider='openrouter', 2 VendorGroups (Anthropic with 2 models, Google with 1 model)
    - groupModelsByProvider with mixed providers returns multiple ModelGroups sorted by provider
    - OpenRouter vendor extraction splits on first '/' only — 'meta-llama/llama-3-70b' → vendor='meta-llama', vendorLabel='Meta Llama'
    - OpenRouter model without '/' in name falls back to provider as vendor
    - Known vendors capitalize correctly: 'anthropic'→'Anthropic', 'openai'→'OpenAI', 'deepseek'→'DeepSeek', 'meta-llama'→'Meta Llama'
    - Unknown vendor 'mistral' capitalizes first letter: 'Mistral'
    - Models within a vendor group are sorted alphabetically by display_name (falling back to name)
  </behavior>
  <action>
    **RED phase:** Create `useModelList.test.ts` with tests for all behaviors listed above. Create empty `groupModelsByProvider` in `useModelList.ts` that returns `[]`. Run tests — all should FAIL except the empty-array test.

    **types/settings.ts** — Create with these exact types (no `any`, named exports only):
    ```typescript
    export type ProviderType = 'vertex_ai' | 'openrouter' | 'ollama';
    export type UseCase = 'chat' | 'embeddings' | 'entity_extraction';

    export interface ModelInfo {
        name: string;
        provider: ProviderType;
        display_name: string | null;
    }

    export interface VendorGroup {
        vendor: string;
        vendorLabel: string;
        models: ModelInfo[];
    }

    export interface ModelGroup {
        provider: ProviderType;
        providerLabel: string;
        vendors: VendorGroup[];
    }

    export interface DefaultModelSetting {
        provider: string;
        model: string;
    }

    export interface ApiKeyStatus {
        provider: string;
        masked_key: string;
        valid: boolean | null;
    }
    ```

    **GREEN phase:** Implement `groupModelsByProvider(models: ModelInfo[]): ModelGroup[]` in `useModelList.ts`:
    1. Group models by `model.provider` into a Map
    2. For each provider group, sub-group by vendor:
       - OpenRouter: extract vendor by splitting `model.name` on first `/`. If no `/`, use provider as vendor.
       - Vertex AI / Ollama: single vendor group with vendor = provider.
    3. Known vendor label map: `{ anthropic: 'Anthropic', google: 'Google', openai: 'OpenAI', deepseek: 'DeepSeek', 'meta-llama': 'Meta Llama' }`. Unknown → capitalize first letter.
    4. Provider label map: `{ vertex_ai: 'Vertex AI', openrouter: 'OpenRouter', ollama: 'Ollama' }`.
    5. Sort models within vendor by `display_name ?? name` (localeCompare).
    6. Sort vendors within provider by `vendorLabel` (localeCompare).

    Also export a `useGroupedModels` hook that combines `useAllModels()` data with `groupModelsByProvider` via `useMemo`.

    **REFACTOR:** Ensure no `any` types. Use `import type` for type-only imports.
  </action>
  <verify>
    <automated>cd AURA-CHAT/client && npx vitest run src/features/settings/hooks/useModelList.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - types/settings.ts exports all 7 types/interfaces with no `any`
    - groupModelsByProvider handles empty, single-provider, multi-provider, OpenRouter vendor splitting, edge cases
    - All test cases pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zustand model store + TanStack Query settings hooks (TDD)</name>
  <files>
    AURA-CHAT/client/src/stores/useModelStore.ts,
    AURA-CHAT/client/src/stores/useModelStore.test.ts,
    AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.ts,
    AURA-CHAT/client/src/features/settings/hooks/useSettingsApi.test.ts
  </files>
  <behavior>
    useModelStore:
    - setSessionModel('sess-1', 'gemini-2.5-flash') stores the model for that session
    - getSessionModel('sess-1') returns 'gemini-2.5-flash' after setSessionModel
    - getSessionModel('unknown-sess') returns undefined
    - clearSessionModel('sess-1') removes the entry; getSessionModel returns undefined
    - Store uses sessionStorage persistence (name: 'aura-model-selection')

    useSettingsApi (settingsKeys):
    - settingsKeys.all returns ['settings']
    - settingsKeys.models() returns ['settings', 'models']
    - settingsKeys.providerModels('vertex_ai') returns ['settings', 'models', 'vertex_ai']
    - settingsKeys.defaults() returns ['settings', 'defaults']
    - settingsKeys.apiKey('openrouter') returns ['settings', 'apiKey', 'openrouter']

    useSettingsApi (hooks — test via QueryClient wrapper):
    - useAllModels fetches from /api/v1/settings/models and returns ModelInfo[]
    - useDefaults fetches from /api/v1/settings/defaults
    - useStoreApiKey mutation calls POST /api/v1/settings/providers/{provider}/api-key and invalidates settingsKeys.models() on success
    - useUpdateDefault mutation calls PUT /api/v1/settings/defaults/{useCase} and invalidates settingsKeys.defaults() on success
  </behavior>
  <action>
    **RED phase:** Create `useModelStore.test.ts` with tests for all store behaviors. Create `useSettingsApi.test.ts` with tests for query key factory and hook behavior (mock axios with `vi.mock('@/lib/api')`). Both test files should fail initially.

    **useModelStore.ts** — GREEN phase:
    ```typescript
    import { create } from 'zustand';
    import { persist, createJSONStorage } from 'zustand/middleware';
    ```
    - State: `sessionModels: Record<string, string>`
    - Actions: `setSessionModel(sessionId, model)`, `getSessionModel(sessionId)`, `clearSessionModel(sessionId)`
    - Persist config: `name: 'aura-model-selection'`, `storage: createJSONStorage(() => sessionStorage)`

    **useSettingsApi.ts** — GREEN phase:
    - Import `api` from `@/lib/api` (the axios instance — use `import api from '@/lib/api'` since it's the default export of the axios instance).
    - Import types from `@/types/settings`.
    - `settingsKeys` factory following the `sessionKeys` pattern from study-sessions.
    - Query hooks (all with proper TypeScript return types):
      - `useAllModels()` → GET `/api/v1/settings/models`, staleTime 5 min, returns `ModelInfo[]`
      - `useDefaults()` → GET `/api/v1/settings/defaults`, staleTime 2 min
      - `useProviderModels(provider)` → GET `/api/v1/settings/providers/${provider}/models`
      - `useApiKeyStatus(provider)` → GET `/api/v1/settings/providers/${provider}/api-key`, returns `ApiKeyStatus`
    - Mutation hooks (with cache invalidation in onSuccess):
      - `useStoreApiKey()` → POST `/api/v1/settings/providers/${provider}/api-key` body `{api_key}`. On success: invalidate `settingsKeys.models()` and `settingsKeys.providerModels(provider)`.
      - `useDeleteApiKey()` → DELETE `/api/v1/settings/providers/${provider}/api-key`. On success: invalidate `settingsKeys.apiKey(provider)`.
      - `useValidateApiKey()` → POST `/api/v1/settings/providers/${provider}/validate`
      - `useUpdateDefault()` → PUT `/api/v1/settings/defaults/${useCase}` body `{provider, model}`. On success: invalidate `settingsKeys.defaults()`.

    Note: The axios instance in `@/lib/api` is NOT a named export — it's the default export of `api`. However the file exports it as a named `api` const and also `export default api`. Use `import api from '@/lib/api'` following the existing codebase pattern in ChatPage.tsx which does `import { getChatConfig } from '@/lib/api'`. For settings hooks, import the `api` default directly.

    Actually, check `lib/api.ts` — it exports individual functions AND the axios instance. The pattern is: functions like `getChatConfig` are named exports using the `api` instance internally. For new endpoints, follow the same pattern: create named exported async functions like `getSettingsModels`, `getSettingsDefaults`, etc., and use them in hooks. OR directly use the `api` instance in hooks. Either approach works — use the direct approach in hooks (it's cleaner and avoids adding to the growing api.ts file).

    **REFACTOR:** Clean up, ensure consistent error handling, no `any`.
  </action>
  <verify>
    <automated>cd AURA-CHAT/client && npx vitest run src/stores/useModelStore.test.ts src/features/settings/hooks/useSettingsApi.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - useModelStore persists per-session model selection to sessionStorage
    - settingsKeys factory produces correct query key arrays
    - All query hooks fetch correct endpoints with proper typing
    - All mutation hooks invalidate appropriate cache keys on success
    - All tests pass with no `any` types
  </done>
</task>

</tasks>

<verification>
Run the full AURA-CHAT client test suite to ensure no regressions:
```bash
cd AURA-CHAT/client && npx vitest run --reporter=verbose
```
All existing tests must still pass, plus new tests for useModelList, useModelStore, useSettingsApi.
</verification>

<success_criteria>
- types/settings.ts exports ProviderType, UseCase, ModelInfo, VendorGroup, ModelGroup, DefaultModelSetting, ApiKeyStatus
- groupModelsByProvider produces correct hierarchies for all provider types
- useModelStore persists model selections keyed by sessionId to sessionStorage
- settingsKeys follows the established query key factory pattern
- All 4 test files pass (useModelList, useModelStore, useSettingsApi)
- No `any` types, named exports only, single quotes
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-provider-settings-model-selection/11-01-SUMMARY.md`
</output>

<objective>
Integrate Study Session UI into existing ChatPage.tsx. Add session sidebar, session management, and session-specific features. Extract reusable components, extend types/api.ts and lib/api.ts.
</objective>

<context>
@.planning/ROADMAP.md - Phase 6 (Frontend Implementation)
@AURA-CHAT/client/src/features/chat/ChatPage.tsx - Integrate session UI here
@AURA-CHAT/client/src/types/api.ts - Extend with session types
@AURA-CHAT/client/src/lib/api.ts - Extend with session API functions
@AURA-CHAT/backend/routers/sessions.py - Session API endpoints

Previous: 06-01 AURA-NOTES-MANAGER Module Browser complete
Previous: 06-02 AURA-CHAT Module Selector complete
Current: AURA-CHAT Study Session UI (integrated into ChatPage)
</context>

<requirements>
1. Session sidebar (collapsible) - session list, create, delete, switch
2. Session-aware chat - messages tied to session, history loading
3. Session management - create new, switch between, delete sessions
4. Extract reusable components from ChatPage.tsx
5. Extend types/api.ts and lib/api.ts

API Integration (verified from backend/routers/sessions.py):
- GET /api/sessions - List user sessions
- POST /api/sessions - Create new session
- GET /api/sessions/{id} - Get single session
- PATCH /api/sessions/{id} - Update session
- DELETE /api/sessions/{id} - Delete session
- GET /api/sessions/{id}/messages - Fetch message history
- POST /api/sessions/{id}/query - Execute RAG query
- GET /api/sessions/{id}/stats - Fetch session statistics
</requirements>

<tasks>
<task>
<type>extract</type>
<file>AURA-CHAT/client/src/components/MessageBubble.tsx</file>
<action>
Extract MessageBubble component from ChatPage.tsx to components/ for reuse.

Purpose:
- Create reusable message bubble component
- Enable reuse after integration
- Avoid duplication

Source:
- Function MessageBubble from features/chat/ChatPage.tsx (lines 433-565)

Features to Preserve:
1. User/assistant message styling differentiation
2. Markdown rendering with ReactMarkdown
3. Citations display with click handlers
4. Thinking content toggle/expansion
5. Loading state with status phases
6. Error display

Imports Required:
- Lucide icons: ChevronDown, AlertCircle
- react-markdown, remarkGfm, rehypeRaw, rehypeSanitize
- useTypewriter from @/hooks/useTypewriter
- cn from @/lib/utils

Props Interface:
interface MessageBubbleProps {
    message: LocalMessage;
    onCitationClick?: (citation: Citation) => void;
}

Integration:
- Export from components/ for import in ChatPage.tsx
- Add LocalMessage type to types/api.ts
</action>
<verify>
1. Web-search: React component extraction patterns
2. npm run build in AURA-CHAT/client
3. Verify ChatPage.tsx import works
</verify>
<done>
MessageBubble extracted to components/</done>
</task>

<task>
<type>extract</type>
<file>AURA-CHAT/client/src/components/CitationPanel.tsx</file>
<action>
Extract CitationPanel component from ChatPage.tsx to components/ for reuse.

Purpose:
- Create reusable citation sidebar component
- Enable reuse after integration

Source:
- Function CitationPanel from features/chat/ChatPage.tsx (lines 569-620)

Features to Preserve:
1. Document title display with serial number badge
2. Chunk text with relevance score
3. Close button
4. Animated slide-in effect (animate-in slide-in-from-right)

Imports Required:
- cn from @/lib/utils
- type Citation from @/types

Props Interface:
interface CitationPanelProps {
    citation: Citation;
    onClose: () => void;
}

Integration:
- Export from components/ for import in ChatPage.tsx
</action>
<verify>
1. Web-search: React animation libraries and slide-in patterns
2. npm run build
3. Verify animation works on open/close
</verify>
<done>
CitationPanel extracted to components/</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/client/src/types/api.ts</file>
<action>
Extend existing types/api.ts with session-related TypeScript interfaces.

Purpose:
- Define interfaces for session data structures
- Ensure type safety across components

Types to Add:
1. LocalMessage (moved from ChatPage.tsx):
   - Extends ChatMessage
   - Properties: id, isLoading, error, statusPhase, thought_summary

2. StudySession:
   - id, title, module_ids, user_id, status, message_count, created_at, updated_at

3. SessionMessage:
   - id, session_id, role, content, created_at, model_used, sources, thinking_content, token_count, message_order

4. SessionStats:
   - session_id, message_count, module_ids, user_messages, assistant_messages, created_at, updated_at

5. SessionQueryRequest:
   - query, enable_thinking

6. SessionQueryResponse:
   - answer, sources, model_used, thought_summary

7. SessionListResponse:
   - sessions: StudySession[], total: number

8. MessageListResponse:
   - messages: SessionMessage[], total: number, has_more: boolean

9. CreateSessionRequest:
   - title, module_ids, user_id

10. UpdateSessionRequest:
    - title?, status?

Preserve existing types (APIResponse, Document types, Chat types, Graph types).
</action>
<verify>
1. Web-search: TypeScript interface extension patterns
2. npx tsc --noEmit
3. Verify types match backend/schemas in routers/sessions.py
</verify>
<done>
types/api.ts extended with session types</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/client/src/lib/api.ts</file>
<action>
Extend existing lib/api.ts with session API functions.

Purpose:
- Add session-related API calls to existing axios client

Functions to Add:
1. getSessions(userId, status?, limit?, offset?) -> SessionListResponse
2. getSession(sessionId, userId) -> StudySession
3. createSession(request: CreateSessionRequest) -> StudySession
4. updateSession(sessionId, userId, updates: UpdateSessionRequest) -> StudySession
5. deleteSession(sessionId, userId) -> { message: string }
6. getSessionMessages(sessionId, userId, limit?, offset?) -> MessageListResponse
7. executeSessionQuery(sessionId, userId, request: SessionQueryRequest) -> SessionQueryResponse
8. getSessionStats(sessionId, userId) -> SessionStats

Use existing axios instance and error handling patterns.

Integration:
- Call /api/sessions endpoints
- Return typed responses using extended types
- Add imports for new types
</action>
<verify>
1. Web-search: axios query parameter handling and TypeScript generics
2. npm run build
3. Verify all API functions match backend endpoint signatures
</verify>
<done>
lib/api.ts extended with session APIs</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/study-sessions/hooks/useStudySession.ts</file>
<action>
Create React Query hook for session CRUD operations.

Purpose:
- Manage server state for session data
- Provide session lifecycle operations

Operations:
1. useSessions(userId, status?) - Query: List sessions
2. useSession(sessionId, userId) - Query: Single session
3. useSessionMessages(sessionId, userId) - Query: Message history
4. useSessionStats(sessionId, userId) - Query: Session statistics
5. useCreateSession() - Mutation: Create session
6. useUpdateSession() - Mutation: Update session
7. useDeleteSession() - Mutation: Delete session

Pattern Reference:
- Follow @/hooks/useGraphQuery.ts for TanStack Query patterns
- Match error handling from existing chat hooks

Query Keys:
- ['sessions', userId, status]
- ['session', sessionId]
- ['sessionMessages', sessionId]
- ['sessionStats', sessionId]

Integration:
- Call extended API functions from lib/api.ts
- Handle loading, error, and success states
- Invalidate queries after mutations
- Export all hooks for use in ChatPage.tsx
</action>
<verify>
1. Web-search: TanStack Query v5 best practices for query invalidation
2. npm run build
3. Verify all hook functions are exported
</verify>
<done>
Session CRUD React Query hooks created</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/study-sessions/hooks/useSessionQuery.ts</file>
<action>
Create React Query hook for RAG query operations within sessions.

Purpose:
- Execute RAG queries in session context
- Manage query state and response
- Extend chat mutation pattern from ChatPage.tsx

Features:
1. useSessionQuery - Mutation for executing queries
2. Loading state during query execution
3. Response handling with answer and sources
4. Error handling for query failures
5. Query invalidation after success (refreshes messages cache)

Pattern Reference:
- See ChatPage.tsx chatMutation (around line 98-130) for existing pattern
- Extend with session_id context

Implementation:
interface UseSessionQueryOptions {
    sessionId: string;
    userId: string;
    onSuccess?: (data: SessionQueryResponse) => void;
    onError?: (error: Error) => void;
}

Returns mutation with:
- mutate(request) - Execute query
- isPending - Loading state
- data - Response data

Integration:
- Extend chatMutation pattern from ChatPage.tsx
- Call POST /api/sessions/{id}/query
- Invalidate ['sessionMessages', sessionId] on success
</action>
<verify>
1. Web-search: React Query mutations for chat applications and optimistic updates
2. npm run build
3. Verify cache invalidation works
</verify>
<done>
Session RAG query hook created</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/study-sessions/components/SessionSidebar.tsx</file>
<action>
Create collapsible SessionSidebar component for session history.

Purpose:
- Display list of user sessions
- Allow session selection and navigation
- Quick access to create new session

Features:
1. Collapsible design (default open, toggle button)
2. List of user sessions ordered by updated_at (descending)
3. Session cards with:
   - Session title (truncate if long)
   - Message count badge
   - Last updated time (format: "2 hours ago", "Yesterday")
   - Active state indicator (border/background)
   - Delete button (confirm dialog)
4. Create new session button
5. Filter by status (All/Active/Archived)
6. Empty state when no sessions
7. Hover effects and transitions

Props Interface:
interface SessionSidebarProps {
    sessions: StudySession[];
    currentSessionId: string | null;
    onSelectSession: (sessionId: string) => void;
    onCreateSession: () => void;
    onDeleteSession: (sessionId: string) => void;
    isOpen: boolean;
    onToggle: () => void;
    isLoading?: boolean;
}

State Management:
- Uses parent callbacks for actions
- No internal state beyond UI interactions

Styling:
- Fixed width (280px) when open
- Hidden/collapsed when closed
- Uses cn() for conditional classes
- AURA cyber theme (primary yellow accents)
</action>
<verify>
1. Web-search: collapsible sidebar patterns and animations
2. npm run build
3. Verify collapse/expand and session navigation
</verify>
<done>
SessionSidebar component created</done>
</task>

<task>
<type>modify</type>
<file>AURA-CHAT/client/src/features/chat/ChatPage.tsx</file>
<action>
Integrate session UI into existing ChatPage.tsx (NOT create separate StudySession.tsx).

Purpose:
- Add session management to existing chat
- Reuse existing chat components and logic
- Single unified chat experience

Layout Changes:
- Add SessionSidebar (collapsible) on left side
- Main chat area remains in center
- CitationPanel slides in from right (existing)

State Additions:
- currentSessionId: string | null
- sidebarOpen: boolean (default: true)

State Modifications:
- messages: Loaded from useSessionMessages when sessionId set
- sessionId: From currentSessionId state (was random ID)

Header Modifications:
- Add sidebar toggle button [☰]
- Add session selector dropdown (current session name)
- Add module selector (existing, keep in place)

Hooks Integration:
- useSessions: Fetch user's sessions on mount
- useSessionMessages: Load messages when session selected
- useSessionQuery: Execute queries in session context
- useCreateSession: Create new sessions
- useDeleteSession: Delete sessions

User Flow:
1. Load page → fetch user's sessions via useSessions
2. No sessions → show empty state with "Create Session" button
3. Create session → POST to /api/sessions, set as currentSessionId
4. Session selected → load messages via useSessionMessages
5. Send query → useSessionQuery, invalidate messages cache
6. Sidebar toggle → show/hide session list
7. Switch session → update currentSessionId, load new messages
8. Delete session → remove from list, clear selection if active

Code Changes Summary:
1. Remove inline MessageBubble (import from components/)
2. Remove inline CitationPanel (import from components/)
3. Add session hooks
4. Add SessionSidebar component
5. Modify message loading logic
6. Add session state
7. Update header with session controls

Preserve Existing:
- Chat input and submit logic
- Mode selector (assistant/tutor)
- Model selection
- Thinking toggle
- Markdown rendering
- Citation handling
</action>
<verify>
1. Web-search: React Query integration patterns with conditional loading
2. npm run build
3. Test end-to-end: create session, send query, switch sessions, delete session
</verify>
<done>
ChatPage.tsx updated with session UI integration</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/06-frontend/06-03-SUMMARY.md`
</output>

<success_criteria>
- [ ] 7 files total (2 extracted + 2 extended + 3 new/modified)
- [ ] TypeScript compiles without errors
- [ ] Collapsible session sidebar in ChatPage
- [ ] Session list displays and navigates correctly
- [ ] Message history loads per session
- [ ] RAG queries execute within session
- [ ] Create/switch/delete sessions work
- [ ] Existing chat functionality preserved
</success_criteria>

<checkpoint:human-verify>
1. Open chat page
2. Create new session
3. Send query, verify response with sources
4. Create another session
5. Switch between sessions
6. Verify message history loads correctly per session
7. Delete a session
8. Verify sidebar collapse/expand works
</checkpoint:human-verify>

# 09-08-PLAN: Phase 09 Integration Testing

<objective>
Validate the complete Phase 09 KG enhancement pipeline through integration testing and benchmarking.

Purpose: Ensure all Phase 09 components work together correctly and meet performance/accuracy targets.

Output: Integration test suite, benchmark results, and phase completion report.
</objective>

<context>
@.planning/KG-ENHANCEMENT-ROADMAP.md - Phase 09 overview and success metrics
@.planning/phases/09-kg-foundation/09-01-PLAN.md through 09-07-PLAN.md - All Phase 09 plans
@AURA-NOTES-MANAGER/api/kg_processor.py - Enhanced KG processor
@AURA-CHAT/backend/ - Reference implementation for comparison

Previous: 09-07 complete (DOCX parsing)
Current: Integration testing and phase validation
</context>

<requirements>
1. Test end-to-end document processing (PDF, TXT, DOCX)
2. Verify hierarchical chunking creates correct parent-child structure
3. Verify entity extraction accuracy ≥95% vs AURA-CHAT baseline
4. Verify relationship extraction with ≥80% precision
5. Verify semantic deduplication reduces duplicates by ≥30%
6. Verify all vector indices operational
7. Performance benchmarks: 10 docs/min throughput
8. Compare output with AURA-CHAT for same input documents
</requirements>

<tasks>
<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/tests/integration/test_kg_enhancement.py</file>
<action>
Create comprehensive integration test suite for Phase 09.

Purpose: Validate all Phase 09 components work together correctly.

Test classes:
1. `TestHierarchicalChunking`
   - test_parent_child_structure
   - test_chunk_token_counts
   - test_embeddings_generated
   - test_relationships_created

2. `TestEntityExtraction`
   - test_four_entity_types_extracted
   - test_confidence_scores_valid
   - test_extraction_from_pdf
   - test_extraction_from_docx
   - test_extraction_from_txt

3. `TestRelationshipExtraction`
   - test_nine_relationship_types
   - test_relationship_confidence_scores
   - test_entity_references_valid

4. `TestSemanticDeduplication`
   - test_similar_entities_merged
   - test_canonical_selection
   - test_relationship_updates
   - test_dedup_reduces_count

5. `TestVectorIndices`
   - test_chunk_vector_search
   - test_parent_chunk_vector_search
   - test_entity_vector_search_all_types

6. `TestDocumentParsing`
   - test_pdf_parsing
   - test_docx_parsing
   - test_txt_parsing
   - test_unsupported_type_error

Use pytest fixtures for:
- Test Neo4j database (can be real or mocked)
- Sample documents (PDF, DOCX, TXT)
- Expected outputs for comparison

Each test should:
- Set up test data
- Run the relevant pipeline component
- Assert expected outcomes
- Clean up test data
</action>
<verify>
1. Run: `python -m py_compile AURA-NOTES-MANAGER/tests/integration/test_kg_enhancement.py`
2. Run: `pytest AURA-NOTES-MANAGER/tests/integration/test_kg_enhancement.py -v`
3. All tests pass
</verify>
<done>
- Integration test file created
- All 6 test classes implemented
- Tests cover all Phase 09 components
- Fixtures for test data
- All tests passing
</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/tests/benchmark/test_kg_performance.py</file>
<action>
Create performance benchmark tests for Phase 09.

Purpose: Measure processing throughput and compare with targets.

Benchmarks:
1. `benchmark_document_processing`
   - Process 10 documents (mix of PDF, DOCX, TXT)
   - Measure total time and throughput (docs/min)
   - Target: ≥10 docs/min

2. `benchmark_entity_extraction`
   - Extract entities from 100 chunks
   - Measure time per chunk
   - Target: <1s per chunk

3. `benchmark_embedding_generation`
   - Generate embeddings for 100 entities
   - Measure batch processing efficiency
   - Target: <0.1s per entity (batched)

4. `benchmark_vector_search`
   - Perform 100 vector searches
   - Measure P99 latency
   - Target: <100ms P99

Use pytest-benchmark for consistent measurement.

Report format:
```
╒═══════════════════════════════════╤════════════╤═══════════════╤══════════╕
│ Benchmark                         │ Mean (ms)  │ P99 (ms)      │ Target   │
├───────────────────────────────────┼────────────┼───────────────┼──────────┤
│ document_processing               │ 5200       │ 6800          │ <6000    │
│ entity_extraction                 │ 850        │ 1200          │ <1000    │
│ embedding_generation              │ 80         │ 120           │ <100     │
│ vector_search                     │ 45         │ 85            │ <100     │
╘═══════════════════════════════════╧════════════╧═══════════════╧══════════╛
```
</action>
<verify>
1. Run: `python -m py_compile AURA-NOTES-MANAGER/tests/benchmark/test_kg_performance.py`
2. Run: `pytest AURA-NOTES-MANAGER/tests/benchmark/test_kg_performance.py --benchmark-only`
3. Verify all benchmarks meet targets
</verify>
<done>
- Benchmark test file created
- All 4 benchmarks implemented
- Results compared against targets
- Performance meets Phase 09 requirements
</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/tests/comparison/compare_with_aura_chat.py</file>
<action>
Create comparison script to validate output against AURA-CHAT baseline.

Purpose: Ensure AURA-NOTES-MANAGER produces equivalent results to AURA-CHAT.

Script functionality:
1. Load same test document in both systems
2. Extract entities from both
3. Compare entity counts, types, and definitions
4. Report accuracy metrics

Metrics to calculate:
- Entity extraction precision
- Entity extraction recall
- Entity extraction F1 score
- Relationship extraction precision/recall

Output format:
```
AURA-NOTES-MANAGER vs AURA-CHAT Comparison
==========================================
Test Document: sample_research_paper.pdf

Entity Extraction:
  AURA-CHAT:          45 entities
  AURA-NOTES-MANAGER: 43 entities
  Matched:            41 entities (91.1%)
  
  By Type:
    Topic:      12 vs 11 (91.7% match)
    Concept:    18 vs 17 (94.4% match)
    Methodology: 8 vs 8 (100% match)
    Finding:     7 vs 7 (100% match)

Relationship Extraction:
  AURA-CHAT:          28 relationships
  AURA-NOTES-MANAGER: 25 relationships
  Matched:            22 relationships (78.6%)

Overall Accuracy: 95.2% (target: ≥95%)
```

Note: This may require manually processing in AURA-CHAT first to get baseline.
</action>
<verify>
1. Run: `python -m py_compile AURA-NOTES-MANAGER/tests/comparison/compare_with_aura_chat.py`
2. Run comparison with sample document
3. Accuracy ≥95% achieved
</verify>
<done>
- Comparison script created
- Metrics calculation implemented
- Entity extraction accuracy ≥95%
- Relationship precision ≥80%
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/09-kg-foundation/09-08-SUMMARY.md`

Include in summary:
- All test results
- Benchmark results vs targets
- Comparison accuracy vs AURA-CHAT
- Any issues discovered and resolved
- Phase 09 completion status
</output>

<success_criteria>
- [ ] Integration tests created and all passing
- [ ] Benchmark tests meet performance targets
- [ ] Entity extraction accuracy ≥95% vs AURA-CHAT
- [ ] Relationship precision ≥80%
- [ ] Processing throughput ≥10 docs/min
- [ ] Vector search latency <100ms P99
- [ ] All Phase 09 components working together
- [ ] py_compile passes for all test files
</success_criteria>

<checkpoint:human-verify>
Review test results and benchmark output:
1. All integration tests passing
2. Benchmarks meet targets (green in report)
3. AURA-CHAT comparison shows ≥95% accuracy
4. No critical issues in test logs
5. Confirm Phase 09 complete and ready for Phase 10
</checkpoint:human-verify>

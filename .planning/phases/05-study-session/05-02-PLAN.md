```
<objective>
Create sessions router in AURA-CHAT/backend/routers/sessions.py with 8 REST endpoints for session lifecycle management, message history retrieval, and RAG query execution within session context.
</objective>

<context>
@.planning/ROADMAP.md - Phase 5 (Study Session System)
@AURA-CHAT/backend/routers/student_modules.py - Existing router pattern (lines 100+)
@AURA-CHAT/backend/session_manager.py - SessionManager (05-01, depends on this)
@AURA-CHAT/backend/rag_engine.py - RAG engine for session queries

Previous: 05-01 SessionManager complete
Current: Session router with endpoints
</context>

<requirements>
1. 8 REST endpoints for session lifecycle management
2. Request/response Pydantic schemas with proper validation
3. SessionManager integration for all session operations
4. User ownership validation on protected endpoints (get, update, delete, messages, stats)
5. Integration with RAGEngine for session-based queries
6. Lazy initialization pattern for SessionManager and RAGEngine

API Endpoints:
| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| GET | /api/sessions | Query param | List user's sessions |
| POST | /api/sessions | Body | Create new session |
| GET | /api/sessions/{id} | Query param | Get session details |
| PATCH | /api/sessions/{id} | Query param | Update session (title/status) |
| DELETE | /api/sessions/{id} | Query param | Delete session and messages |
| GET | /api/sessions/{id}/messages | Query param | Get message history |
| POST | /api/sessions/{id}/query | Query param | Execute RAG query |
| GET | /api/sessions/{id}/stats | Query param | Get session analytics |

Router Configuration:
- Prefix: /api/sessions
- Tags: ["Study Sessions"]
- user_id passed as query parameter for ownership validation
</requirements>

<tasks>
<task>
<type>create</type>
<file>AURA-CHAT/backend/routers/sessions.py</file>
<action>
Create sessions.py router with all endpoints.

Purpose:
- Expose SessionManager operations via REST API
- Handle request validation with Pydantic schemas
- Validate user ownership on protected operations
- Integrate RAGEngine for session-based queries

Router Setup:
- Create FastAPI APIRouter with prefix="/api/sessions" and tags=["Study Sessions"]
- Implement lazy initialization pattern with global _session_manager and _rag_engine
- Create helper functions: get_session_manager(), get_rag_engine()
- Create initialization functions: initialize_session_manager(), initialize_rag_engine()

Request/Response Schemas:
Create Pydantic models for:
- CreateSessionRequest: title (str), module_ids (List[str]), user_id (str)
- SessionResponse: id, title, module_ids, user_id, status, message_count, created_at, updated_at
- UpdateSessionRequest: title (Optional[str]), status (Optional[str])
- AddMessageRequest: role (str), content (str), model_used (Optional[str]), sources (Optional[List[str]]), thinking_content (Optional[str]), token_count (Optional[int])
- MessageResponse: id, session_id, role, content, created_at, model_used, sources, thinking_content, token_count
- QueryRequest: query (str), enable_thinking (bool)
- QueryResponse: answer (str), sources (List[dict]), model_used (str), thought_summary (Optional[str])
- StatsResponse: session_id, message_count, module_ids, user_messages, assistant_messages, created_at, updated_at
- SessionListResponse: sessions (List[SessionResponse]), total (int)
- MessageListResponse: messages (List[MessageResponse]), total (int), has_more (bool)

Session Endpoints:
1. GET / - List user sessions
   - Query: user_id (str), status (Optional[str])
   - Returns: SessionListResponse

2. POST / - Create new session
   - Body: CreateSessionRequest
   - Returns: SessionResponse (201)
   - Validates module_ids is not empty

3. GET /{session_id} - Get session details
   - Path: session_id
   - Query: user_id
   - Returns: SessionResponse
   - Validates ownership

4. PATCH /{session_id} - Update session
   - Path: session_id
   - Query: user_id
   - Body: UpdateSessionRequest
   - Returns: SessionResponse
   - Validates ownership and status values

5. DELETE /{session_id} - Delete session
   - Path: session_id
   - Query: user_id
   - Returns: {message: "Session deleted"}
   - Validates ownership

Message Endpoints:
6. GET /{session_id}/messages - Get message history
   - Path: session_id
   - Query: user_id, limit (int, default 100), offset (int, default 0)
   - Returns: MessageListResponse
   - Validates ownership

7. POST /{session_id}/messages - Add message
   - Path: session_id
   - Query: user_id
   - Body: AddMessageRequest
   - Returns: MessageResponse (201)
   - Validates ownership and role is "user" or "assistant"

Query Endpoint:
8. POST /{session_id}/query - Execute RAG query in session context
   - Path: session_id
   - Query: user_id
   - Body: QueryRequest
   - Returns: QueryResponse
   - Validates ownership
   - Gets session module_ids
   - Adds user message to history
   - Executes RAG query with module filtering
   - Adds assistant message with response
   - Returns answer with sources

Error Handling:
- HTTPException 400 for validation errors
- HTTPException 401 for ownership failures
- HTTPException 404 for not found
- HTTPException 503 for uninitialized managers
</action>
<verify>
1. Web-search: Research FastAPI router patterns, Pydantic validation best practices, and async session management
2. Check syntax: `python -m py_compile AURA-CHAT/backend/routers/sessions.py`
3. Verify all 8 endpoints have proper decorators
4. Verify all Pydantic schemas are defined
</verify>
<done>
Sessions router with 8 endpoints created
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/05-study-session/05-02-SUMMARY.md`
</output>

<success_criteria>
- [ ] 8 session endpoints implemented with proper decorators
- [ ] GET /api/sessions returns user sessions with pagination
- [ ] POST /api/sessions creates new session
- [ ] GET /api/sessions/{id}/messages returns message history
- [ ] POST /api/sessions/{id}/query executes RAG within session
- [ ] GET /api/sessions/{id}/stats returns analytics
- [ ] User ownership validation on protected endpoints
- [ ] All files pass py_compile check
</success_criteria>

<checkpoint:human-verify>
Test session endpoints via Swagger UI at /docs (AURA-CHAT):
1. Create session: POST /api/sessions with {title, module_ids, user_id}
2. Add query: POST /api/sessions/{id}/query with {query}
3. Get messages: GET /api/sessions/{id}/messages
4. View stats: GET /api/sessions/{id}/stats
</checkpoint:human-verify>

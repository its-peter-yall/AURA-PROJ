---
phase: 13-polish-integration-testing
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CONFIG-01, CONFIG-03, CONFIG-04, UI-03]

must_haves:
  truths:
    - "CONFIG-01 verified: admin settings endpoints exist for setting default provider/model per use case"
    - "CONFIG-03 verified: model cache dynamically discovers available models with configurable TTL"
    - "CONFIG-04 verified: API key management endpoints store, validate, and mask keys per provider"
    - "UI-03 verified: no direct provider SDK imports exist outside shared/model_router"
    - "REQUIREMENTS.md traceability table shows all 16 requirements as Complete with correct phase attribution"
    - "All new integration tests + existing suites pass in a single final run"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with CONFIG-01, CONFIG-03, CONFIG-04, UI-03 marked Complete"
      contains: "16/16"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 plans list and status updated"
      contains: "13-01-PLAN.md"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "AURA-CHAT/server/routers/settings.py"
      via: "CONFIG-01 implementation evidence"
      pattern: "settings.*default"
    - from: ".planning/REQUIREMENTS.md"
      to: "shared/model_router/src/model_router/model_cache.py"
      via: "CONFIG-03 implementation evidence"
      pattern: "MODEL_CACHE_TTL"
    - from: ".planning/REQUIREMENTS.md"
      to: "shared/model_router/src/model_router/key_manager.py"
      via: "CONFIG-04 implementation evidence"
      pattern: "KeyManager|mask"
---

<objective>
Verify the four "Pending" requirements (CONFIG-01, CONFIG-03, CONFIG-04, UI-03) are actually implemented, update the traceability table in REQUIREMENTS.md, and run a final all-suites-green validation confirming the entire multi-provider system is production-ready.

Purpose: The REQUIREMENTS.md traceability table is stale — these requirements were implemented in Phases 10-11 but never marked complete. This plan closes the documentation gap and performs the final gate check for v1.1.

Output: Updated REQUIREMENTS.md with all 16 requirements marked Complete, updated ROADMAP.md with Phase 13 plan list, final validation proof.
</objective>

<execution_context>
@C:/Users/Peter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-polish-integration-testing/13-RESEARCH.md

<!-- Prior plan summaries needed for evidence chain -->
@.planning/phases/10-cross-app-migration-backend-integration/10-01-SUMMARY.md
@.planning/phases/10-cross-app-migration-backend-integration/10-02-SUMMARY.md
@.planning/phases/10-cross-app-migration-backend-integration/10-04-SUMMARY.md
@.planning/phases/10-cross-app-migration-backend-integration/10-06-SUMMARY.md

<interfaces>
<!-- Implementation evidence locations from prior phases -->

CONFIG-01 (Admin default provider/model per use case):
  - AURA-CHAT: server/routers/settings.py — GET/PUT /api/settings/defaults
  - AURA-NOTES-MANAGER: api/routers/settings.py — GET/PUT /api/settings/defaults
  - Shared: model_router/settings_store.py — SettingsStore with Redis-backed defaults
  - Test: server/tests/test_settings_router.py

CONFIG-03 (Dynamic model discovery with cached lists):
  - Shared: model_router/model_cache.py — ModelCache with configurable TTL
  - Config: MODEL_CACHE_TTL_SECONDS env var (300-3600 seconds)
  - Endpoints: GET /api/settings/models in both apps
  - Test: shared/model_router/tests/test_model_cache.py

CONFIG-04 (Secure API key management with masked display):
  - Shared: model_router/key_manager.py — KeyManager with Fernet encryption
  - Endpoints: GET/PUT/DELETE /api/settings/keys/{provider} in both apps
  - Masked response: sk-...abc pattern
  - Test: shared/model_router/tests/test_key_manager.py

UI-03 (No direct provider imports):
  - Test: tests/test_no_direct_imports.py — AST-based scanner
  - Covers: AURA-CHAT (backend + server), AURA-NOTES-MANAGER (api + services), test dirs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pending requirements against codebase + update traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Verify each pending requirement by confirming the implementation exists in code:

**CONFIG-01 — Admin default provider/model per use case:**
1. `grep -n "defaults" AURA-CHAT/server/routers/settings.py` — confirm GET/PUT endpoints
2. `grep -n "defaults" AURA-NOTES-MANAGER/api/routers/settings.py` — confirm same
3. `grep -n "SettingsStore" shared/model_router/src/model_router/settings_store.py` — confirm class exists
4. Mark as Complete (Phase 10) in traceability table

**CONFIG-03 — Dynamic model discovery with cached model lists:**
1. `grep -n "ModelCache" shared/model_router/src/model_router/model_cache.py` — confirm class
2. `grep -n "MODEL_CACHE_TTL" shared/model_router/` — confirm configurable TTL
3. `grep -n "models" AURA-CHAT/server/routers/settings.py` — confirm model listing endpoint
4. Mark as Complete (Phase 10) in traceability table

**CONFIG-04 — Secure API key management with masked display:**
1. `grep -n "KeyManager" shared/model_router/src/model_router/key_manager.py` — confirm class
2. `grep -n "mask" shared/model_router/src/model_router/key_manager.py` — confirm masking
3. `grep -n "keys" AURA-CHAT/server/routers/settings.py` — confirm key endpoints
4. Mark as Complete (Phase 10) in traceability table

**UI-03 — No direct provider imports:**
1. `grep -n "FORBIDDEN_PATTERNS" tests/test_no_direct_imports.py` — confirm scanner
2. Run: `.venv/Scripts/python -m pytest tests/test_no_direct_imports.py -v` — confirm passes
3. Mark as Complete (Phase 10) in traceability table

**Update REQUIREMENTS.md:**
- Change all four `- [ ]` to `- [x]` for CONFIG-01, CONFIG-03, CONFIG-04, UI-03
- Update traceability table: change Status from "Pending" to "Complete" for all four
- Update the Coverage section to show 16/16 complete
- Add note: "All requirements verified complete as of Phase 13 (2026-03-11)"

If ANY requirement is NOT actually implemented (missing endpoint, missing feature), document the gap and create a TODO in STATE.md. Do NOT mark it complete if it's not.
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_no_direct_imports.py -v --tb=short</automated>
  </verify>
  <done>All four pending requirements verified against code. REQUIREMENTS.md updated with all 16 requirements marked Complete (or gaps documented if found). Traceability table is current.</done>
</task>

<task type="auto">
  <name>Task 2: Final all-suites validation + planning docs update</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**Step 1 — Final combined test run (all suites):**
Run all test suites in sequence to confirm everything is green after Plans 01 + 02 + this plan's Task 1:
```bash
# Shared model_router (including new integration tests from 13-01)
cd shared/model_router && ../../.venv/Scripts/python -m pytest tests/ --tb=short -q

# Cross-app integration + performance (from 13-01)
cd ../.. && .venv/Scripts/python -m pytest tests/ --tb=short -q

# AURA-CHAT backend
cd AURA-CHAT && ../.venv/Scripts/python -m pytest tests/ server/tests/ --tb=short -q

# AURA-CHAT frontend
cd client && npx vitest run --reporter=verbose 2>&1 | tail -5

# Back to root
cd ../..
```
All suites must pass. If any fail, diagnose and fix (or document as known pre-v1.1 issue).

**Step 2 — Update ROADMAP.md Phase 13 section:**
Replace the TBD plan list with:
```
Plans:
- [ ] 13-01-PLAN.md — Cross-provider integration tests + router overhead benchmark
- [ ] 13-02-PLAN.md — Full regression sweep across all test suites
- [ ] 13-03-PLAN.md — Requirements verification + traceability closure
```
Update `**Plans:** 3 plans`.

**Step 3 — Update STATE.md:**
- Update `stopped_at` to reflect Phase 13 in progress
- Update `last_activity` 
- Update progress: `completed_phases: 5` (Phase 13 not yet complete until all plans done)
- Add note in Pending Todos if any issues were found
  </action>
  <verify>
    <automated>cd shared/model_router && ../../.venv/Scripts/python -m pytest tests/ --tb=short -q && cd ../.. && .venv/Scripts/python -m pytest tests/ --tb=short -q</automated>
  </verify>
  <done>All test suites green in a single final run. ROADMAP.md reflects Phase 13 plan structure. STATE.md reflects current progress. The v1.1 multi-provider architecture is fully validated.</done>
</task>

</tasks>

<verification>
```bash
# Final gate: ALL suites must pass
cd shared/model_router && ../../.venv/Scripts/python -m pytest tests/ --tb=short -q
cd ../..
.venv/Scripts/python -m pytest tests/ --tb=short -q
cd AURA-CHAT && ../.venv/Scripts/python -m pytest tests/ server/tests/ --tb=short -q
cd client && npx vitest run 2>&1 | tail -5
cd ../..

# Confirm REQUIREMENTS.md is fully up to date
grep -c "Complete" .planning/REQUIREMENTS.md
# Should show 16 (all requirements)
```
</verification>

<success_criteria>
- REQUIREMENTS.md: All 16 v1.1 requirements marked Complete with correct phase attribution
- ROADMAP.md: Phase 13 plan list populated with 3 plans
- All test suites pass in final combined run (shared + cross-app + AURA-CHAT + AURA-NOTES)
- No stale "Pending" markers remain in requirements traceability
- STATE.md reflects Phase 13 in progress
</success_criteria>

<output>
After completion, create `.planning/phases/13-polish-integration-testing/13-03-SUMMARY.md`
</output>

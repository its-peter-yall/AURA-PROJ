---
phase: 17-frontend-cross-app-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AURA-NOTES-MANAGER/frontend/src/types/settings.ts
  - AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []
must_haves:
  truths:
    - "AURA-NOTES-MANAGER settings page shows 5 use case rows (chat, embeddings, entity_extraction, gatekeeper, relationship_extraction)"
    - "Admin can select a provider/model for gatekeeper via the picker"
    - "Admin can select a provider/model for relationship_extraction via the picker"
    - "TypeScript compiles with no errors after changes"
  artifacts:
    - path: "AURA-NOTES-MANAGER/frontend/src/types/settings.ts"
      provides: "UseCase type union including gatekeeper and relationship_extraction"
      contains: "'gatekeeper' | 'relationship_extraction'"
    - path: "AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx"
      provides: "USE_CASES array with 5 entries and USE_CASE_MODEL_TYPES with 5 mappings"
      contains: "gatekeeper"
      contains: "relationship_extraction"
  key_links:
    - from: "UseCase type"
      to: "USE_CASES array"
      via: "TypeScript type constraint on id field"
      pattern: "id: UseCase"
    - from: "USE_CASES array"
      to: "USE_CASE_MODEL_TYPES record"
      via: "Complete record mapping for all UseCase values"
      pattern: "Record<UseCase,"
    - from: "USE_CASES array"
      to: "HierarchicalModelPicker"
      via: "USE_CASE_MODEL_TYPES filters models by type per use case"
      pattern: "USE_CASE_MODEL_TYPES\\[useCase.id\\]"
---

<objective>
Expand AURA-NOTES-MANAGER settings page to show `gatekeeper` and `relationship_extraction` use case rows by extending the UseCase type union, USE_CASES array, and USE_CASE_MODEL_TYPES record.

Purpose: API-01 and API-02 require admins to configure gatekeeper and relationship_extraction models via the settings page. The backend already supports these use cases — only the frontend types and component arrays need updating.

Output: Updated type file + component array with all 5 use cases visible and functional in the settings page.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-frontend-cross-app-validation/17-UI-SPEC.md
@.planning/phases/17-frontend-cross-app-validation/17-RESEARCH.md

<interfaces>
From AURA-NOTES-MANAGER/frontend/src/types/settings.ts (current):
```typescript
export type ProviderType = 'vertex_ai' | 'openrouter' | 'ollama';
export type UseCase = 'chat' | 'embeddings' | 'entity_extraction';
export interface ModelInfo { name: string; provider: ProviderType; display_name: string | null; model_type: 'generation' | 'embedding'; }
export interface DefaultModelSetting { provider: string; model: string; }
```

From AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx (current, lines 44-54):
```typescript
const USE_CASES: { id: UseCase; label: string; description: string }[] = [
    { id: 'chat', label: 'Chat Model', description: 'Used for conversational responses and RAG' },
    { id: 'embeddings', label: 'Embeddings Model', description: 'Used for document indexing and vector search' },
    { id: 'entity_extraction', label: 'Entity Extraction Model', description: 'Used for building knowledge graphs from documents' }
];
const USE_CASE_MODEL_TYPES: Record<UseCase, 'generation' | 'embedding'> = {
    chat: 'generation',
    embeddings: 'embedding',
    entity_extraction: 'generation',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UseCase type in NOTES-MANAGER settings types</name>
  <read_first>AURA-NOTES-MANAGER/frontend/src/types/settings.ts</read_first>
  <files>AURA-NOTES-MANAGER/frontend/src/types/settings.ts</files>
  <action>
    In `AURA-NOTES-MANAGER/frontend/src/types/settings.ts`, change line 32 from:
    `export type UseCase = 'chat' | 'embeddings' | 'entity_extraction';`
    to:
    `export type UseCase = 'chat' | 'embeddings' | 'entity_extraction' | 'gatekeeper' | 'relationship_extraction';`

    No other changes to this file.
  </action>
  <verify>
    ```bash
    cd AURA-NOTES-MANAGER/frontend && npx tsc --noEmit
    ```
  </verify>
  <acceptance_criteria>
    - File contains line: `export type UseCase = 'chat' | 'embeddings' | 'entity_extraction' | 'gatekeeper' | 'relationship_extraction';`
    - TypeScript compiles with zero errors via `npx tsc --noEmit` in AURA-NOTES-MANAGER/frontend/
    - No other lines in the file were changed
  </acceptance_criteria>
  <done>UseCase type union includes all 5 use cases, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add gatekeeper and relationship_extraction to USE_CASES and USE_CASE_MODEL_TYPES</name>
  <read_first>AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx</read_first>
  <files>AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx</files>
  <action>
    In `AURA-NOTES-MANAGER/frontend/src/features/settings/components/DefaultModelSection.tsx`:

    1. Expand the USE_CASES array (currently lines 44-48) from 3 entries to 5. Add after the entity_extraction entry:
    ```typescript
    { id: 'gatekeeper', label: 'Gatekeeper Model', description: 'Used for query validation and access control' },
    { id: 'relationship_extraction', label: 'Relationship Extraction Model', description: 'Used for extracting relationships between entities in documents' }
    ```

    2. Expand USE_CASE_MODEL_TYPES (currently lines 50-54) from 3 to 5 entries. Add:
    ```typescript
    gatekeeper: 'generation',
    relationship_extraction: 'generation',
    ```

    Both gatekeeper and relationship_extraction use 'generation' type (not 'embedding') because they generate text responses via LLM, not produce vector embeddings.
  </action>
  <verify>
    ```bash
    cd AURA-NOTES-MANAGER/frontend && npx tsc --noEmit
    ```
  </verify>
  <acceptance_criteria>
    - USE_CASES array contains exactly 5 entries with ids: chat, embeddings, entity_extraction, gatekeeper, relationship_extraction
    - Gatekeeper entry has label: 'Gatekeeper Model' and description: 'Used for query validation and access control'
    - Relationship extraction entry has label: 'Relationship Extraction Model' and description: 'Used for extracting relationships between entities in documents'
    - USE_CASE_MODEL_TYPES record maps gatekeeper to 'generation'
    - USE_CASE_MODEL_TYPES record maps relationship_extraction to 'generation'
    - TypeScript compiles with zero errors via `npx tsc --noEmit`
  </acceptance_criteria>
  <done>All 5 use cases visible in NOTES-MANAGER settings page with correct model type filtering</done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` in AURA-NOTES-MANAGER/frontend/ — must pass with zero errors
2. Run `npm run build` in AURA-NOTES-MANAGER/frontend/ — must succeed
3. Grep for 'gatekeeper' and 'relationship_extraction' in both modified files — must appear in type union, USE_CASES array, and USE_CASE_MODEL_TYPES record
</verification>

<success_criteria>
- UseCase type union contains all 5 use case strings
- USE_CASES array has 5 entries with correct labels and descriptions matching UI-SPEC.md copywriting contract
- USE_CASE_MODEL_TYPES record has 5 entries with correct model_type mappings
- TypeScript type checking passes with no errors
- Build succeeds without warnings related to these changes
</success_criteria>

<output>
After completion, create `.planning/phases/17-frontend-cross-app-validation/17-01-SUMMARY.md`
</output>

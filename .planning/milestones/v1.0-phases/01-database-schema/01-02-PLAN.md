<objective>
Define StudySession and Message node types with constraints in the database migration.
</objective>

<context>
@.planning/BRIEF.md - Project vision
@.planning/ROADMAP.md - Phase 1 requirements
@.planning/phases/01-database-schema/01-01-SUMMARY.md - Previous task outcome
@Final-Module-2-KG.md - StudySession and Message node specifications

Previous: Module node constraints created
Current: Need to add StudySession and Message nodes
</context>

<requirements>
1. Add StudySession node type with properties from ROADMAP
2. Add Message node type with properties from ROADMAP
3. Create constraints for StudySession.id and Message.id
4. Create indexes for session lookups by user_id
5. Extend existing migration script or create new version
6. Define relationships: (StudySession)-[:HAS_MESSAGE]->(Message)
7. Define relationships: (StudySession)-[:STUDIES]->(Module)
</requirements>

<tasks>
<task>
<type>modify</type>
<file>AURA-NOTES-MANAGER/api/migrations/001_add_module_schema.py</file>
<action>
Add to migration script:
- StudySession node definition with properties:
  - id (String!, UUID)
  - title (String!)
  - module_ids ([String]!)
  - user_id (String!)
  - status (String!)
  - message_count (Integer)
  - settings (String)
  - created_at (DateTime!)
  - updated_at (DateTime!)
  - is_active (Boolean!)
- Message node definition with properties:
  - id (String!)
  - session_id (String!)
  - role (String!)
  - content (String!)
  - created_at (DateTime!)
  - model_used (String)
  - sources ([String])
  - thinking_content (String)
  - token_count (Integer)
- Create unique constraints for StudySession.id and Message.id
- Create indexes for StudySession.user_id, StudySession.status, Message.session_id
</action>
<verify>
Run migration:
- Check constraints: `SHOW CONSTRAINTS`
- Test StudySession creation
- Test Message creation
- Verify relationship types can be created
</verify>
<done>
All StudySession and Message constraints and indexes created
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/01-database-schema/01-02-SUMMARY.md`
</output>

<success_criteria>
- [ ] StudySession.id unique constraint created
- [ ] Message.id unique constraint created
- [ ] StudySession.user_id index created
- [ ] Message.session_id index created
- [ ] All constraints verified working
</success_criteria>

<checkpoint:human-verify>
Verify node creation and relationship capability
</checkpoint:human-verify>

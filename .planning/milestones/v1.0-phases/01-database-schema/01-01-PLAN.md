<objective>
Create database migration script that adds Module node type with constraints to the Neo4j graph database.
</objective>

<context>
@.planning/BRIEF.md - Project vision and scope
@.planning/ROADMAP.md - Phase 1 requirements (Week 1-2)
@Final-Module-2-KG.md - Source specification document

Current state: No migration script exists. Database schema needs Module, StudySession, Message nodes with constraints.
</context>

<requirements>
1. Create migration script at `backend/migrations/001_add_module_schema.py`
2. Define Module node with properties from ROADMAP
3. Create unique constraint on Module.id
4. Create index on Module.user_id for ownership queries
5. Script must be idempotent (use `IF NOT EXISTS`)
6. Include migration version and timestamp
7. Add rollback capability or forward-only design
</requirements>

<tasks>
<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/api/migrations/001_add_module_schema.py</file>
<action>
Create migration script with:
- Migration class inheriting from base Migration class
- `upgrade()` method to apply changes
- `downgrade()` method to rollback (optional)
- Neo4j driver connection handling
- Idempotent constraint creation using `CREATE CONSTRAINT ... IF NOT EXISTS`
- Logging for progress tracking
</action>
<verify>
Run script against test Neo4j instance:
- Check constraints created: `SHOW CONSTRAINTS`
- Verify Module node can be created: `CREATE (m:Module {id: 'test', name: 'Test', user_id: 'user1'})`
- Clean up test data
</verify>
<done>
Migration script exists at correct path and creates constraints without errors
</done>
</task>

<task>
<type>create</type>
<file>AURA-NOTES-MANAGER/api/migrations/__init__.py</file>
<action>
Create migrations package init file if it doesn't exist
</action>
<verify>
Python can import: `from backend.migrations import Migration`
</verify>
<done>
Package is importable
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/01-database-schema/01-01-SUMMARY.md`
</output>

<success_criteria>
- [ ] Migration script created at `backend/migrations/001_add_module_schema.py`
- [ ] Module.id unique constraint created
- [ ] Module.user_id index created
- [ ] Script is idempotent (re-running succeeds)
- [ ] No errors on execution
</success_criteria>

<checkpoint:human-verify>
Verify constraints created: `SHOW CONSTRAINTS` output matches expected
</checkpoint:human-verify>

```
<objective>
Create Hierarchical Module Selector UI for AURA-CHAT student portal with department → semester → subject → module single-select drill-down. Extend existing types/api.ts and lib/api.ts, reuse Dropdown component from ChatPage.tsx. Students chat with one module at a time.
</objective>

<context>
@.planning/ROADMAP.md - Phase 6 (Frontend Implementation)
@AURA-CHAT/client/src/types/api.ts - Extend with module types
@AURA-CHAT/client/src/lib/api.ts - Extend with module API functions
@AURA-CHAT/client/src/features/chat/ChatPage.tsx - Reuse Dropdown component
@AURA-CHAT/backend/routers/student_modules.py - Available modules API

Previous: 06-01 AURA-NOTES-MANAGER Module Browser complete
Current: AURA-CHAT Hierarchical Module Selector UI
</context>

<requirements>
1. Feature-based structure under features/modules/
2. Hierarchical drill-down: Department → Semester → Subject → Module
3. Single selection at each level (one module at a time for chat)
4. Module cards with key info (code, name, description)
5. React Query hooks for module data
6. Extend types/api.ts with module types (not new file)
7. Extend lib/api.ts with module API functions (not new file)

Reuse Strategy:
- types/api.ts: Add Module, Department, Semester, Subject types
- lib/api.ts: Add getDepartments, getSemesters, getSubjects, getModules functions
- features/chat/ChatPage.tsx: Reuse Dropdown component for selectors

New Files to Create (6 total):
- features/modules/hooks/useModule.ts
- features/modules/components/DepartmentSelector.tsx
- features/modules/components/SemesterSelector.tsx
- features/modules/components/SubjectSelector.tsx
- features/modules/components/ModuleCard.tsx
- features/modules/components/ModuleSelector.tsx

Extended Files (2 total):
- types/api.ts - Add module-related types
- lib/api.ts - Add module API functions

API Integration:
- GET /api/student/modules/departments - Fetch departments
- GET /api/student/modules/semesters?department_id=xxx - Fetch semesters for department
- GET /api/student/modules/subjects?department_id=xxx&semester_id=xxx - Fetch subjects
- GET /api/student/modules?department_id=xxx&semester_id=xxx&subject_id=xxx - Fetch modules
</requirements>

<tasks>
<task>
<type>update</type>
<file>AURA-CHAT/client/src/types/api.ts</file>
<action>
Extend existing types/api.ts with module-related TypeScript interfaces.

Purpose:
- Define interfaces for module data structures
- Ensure type safety across components and API responses
- Avoid creating duplicate type files

Types to Add:
1. Department: id, name, code
2. Semester: id, name, year, semester_number, department_id
3. Subject: id, name, code, department_id, semester_id, module_count
4. Module interface: id, code, name, description, year, semester, document_count, published_at, subject_id, department_id, semester_id
5. ModuleResponse: API response for single module
6. ModuleListResponse: API response for module list
7. DepartmentListResponse, SemesterListResponse, SubjectListResponse: API responses for hierarchy

Preserve existing types (APIResponse, Document types, Chat types, Graph types).

Export types for use in components and hooks.
</action>
<verify>
1. Web-search: Research TypeScript interface extension patterns
2. Check syntax: Verify TypeScript compilation
3. Verify all type interfaces are complete
</verify>
<done>
types/api.ts extended with module types including hierarchy (Department, Semester, Subject, Module)
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/client/src/lib/api.ts</file>
<action>
Extend existing lib/api.ts with module API functions.

Purpose:
- Add module-related API calls to existing axios client
- Avoid creating separate API file

Functions to Add:
1. getDepartments() - Fetch all departments
2. getSemesters(department_id) - Fetch semesters for a department
3. getSubjects(department_id, semester_id) - Fetch subjects for semester
4. getModules(department_id, semester_id, subject_id) - Fetch modules for subject

Use existing axios instance and error handling patterns.

Integration:
- Call /api/student/modules endpoints with appropriate query params
- Return typed responses using extended types
</action>
<verify>
1. Web-search: Research axios API client patterns
2. Check syntax: Verify TypeScript compilation
3. Verify all API functions are exported
</verify>
<done>
lib/api.ts extended with module API functions including hierarchical endpoints
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/hooks/useModule.ts</file>
<action>
Create React Query hook for module data fetching.

Purpose:
- Manage server state for module data
- Handle hierarchical data fetching (departments → semesters → subjects → modules)

Operations:
1. useDepartments - Fetch all departments
2. useSemesters(department_id) - Fetch semesters for selected department
3. useSubjects(department_id, semester_id) - Fetch subjects for selected semester
4. useModules(department_id, semester_id, subject_id) - Fetch modules for selected subject

State Management:
- Store selected hierarchy (department_id, semester_id, subject_id) in local storage
- Each level cached independently
- Auto-fetch next level when parent is selected

Integration:
- Call extended API functions from lib/api.ts
- Handle loading, error, and success states
</action>
<verify>
1. Web-search: Research React Query hooks for hierarchical data
2. Check syntax: Verify TypeScript compilation
3. Verify all hook functions are exported
</verify>
<done>
Module React Query hooks created with hierarchical data support
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/components/DepartmentSelector.tsx</file>
<action>
Create DepartmentSelector component for the first level of hierarchy.

Purpose:
- Allow students to select their department
- Filter subsequent choices to relevant departments

Features:
1. Display list of available departments
2. Visual feedback for selected department
3. Click handler for selection
4. Loading state while fetching
5. Error state if fetch fails

Props Interface:
- selectedDepartmentId: Currently selected department ID
- onSelect: Callback when department is selected

Styling:
- Card-based layout or list view
- Highlight selected department
- Hover effects for interactivity
</action>
<verify>
1. Web-search: Research React selector component patterns
2. Check syntax: Verify TypeScript compilation
3. Verify selection visual feedback works
</verify>
<done>
DepartmentSelector component created
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/components/SemesterSelector.tsx</file>
<action>
Create SemesterSelector component for the second level of hierarchy.

Purpose:
- Display semesters available for selected department
- Allow students to select their semester/year

Features:
1. Display list of semesters for department
2. Visual feedback for selected semester
3. Click handler for selection
4. Disabled state when no department selected
5. Loading state while fetching
6. Error state if fetch fails

Props Interface:
- departmentId: Parent department ID for filtering
- selectedSemesterId: Currently selected semester ID
- onSelect: Callback when semester is selected

Styling:
- Card-based layout or timeline view
- Show year and semester number
- Highlight selected semester
</action>
<verify>
1. Web-search: Research React selector component patterns
2. Check syntax: Verify TypeScript compilation
3. Verify filtering by department works
</verify>
<done>
SemesterSelector component created
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/components/SubjectSelector.tsx</file>
<action>
Create SubjectSelector component for the third level of hierarchy.

Purpose:
- Display subjects available for selected semester
- Allow students to select a subject to view modules

Features:
1. Display list of subjects for semester
2. Visual feedback for selected subject
3. Click handler for single selection
4. Disabled state when no semester selected
5. Loading state while fetching
6. Show module count per subject

Props Interface:
- departmentId: Parent department ID
- semesterId: Parent semester ID for filtering
- selectedSubjectId: Currently selected subject ID (single)
- onSelect: Callback when subject is selected

Styling:
- Card-based layout or list view
- Show subject code, name, and module count
- Highlight selected subject
</action>
<verify>
1. Web-search: Research React selector component patterns
2. Check syntax: Verify TypeScript compilation
3. Verify single subject selection works
</verify>
<done>
SubjectSelector component created
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/components/ModuleCard.tsx</file>
<action>
Create ModuleCard component for displaying module in selector.

Purpose:
- Display module information in selection context
- Visual feedback for selected/unselected state

Features:
1. Display module code, name, and description
2. Show document count and semester/year
3. Selection checkbox or indicator
4. Click handler for toggle selection
5. Disabled state when not available

Props Interface:
- module: Module data (from extended types)
- selected: Boolean for selection state
- onToggle: Selection toggle callback
- disabled: Optional disabled state

Styling:
- Visual distinction for selected vs unselected
- Hover effects for interactivity
- Disabled styling when unavailable
</action>
<verify>
1. Web-search: Research card component with selection patterns
2. Check syntax: Verify TypeScript compilation
3. Verify selection visual feedback works
</verify>
<done>
ModuleCard component created
</done>
</task>

<task>
<type>create</type>
<file>AURA-CHAT/client/src/features/modules/components/ModuleSelector.tsx</file>
<action>
Create ModuleSelector component as hierarchical dropdown.

Purpose:
- Allow students to drill down: Department → Semester → Subject → Module
- Single module selection for chat context
- Reuse Dropdown component from ChatPage.tsx

Hierarchical Flow:
1. Department Selection (required, first level)
2. Semester Selection (filtered by department)
3. Subject Selection (filtered by semester, single select)
4. Module Selection (filtered by subject, single select)

Features:
1. Dropdown button showing selected module summary
2. Panel with 4-level hierarchy navigation
3. Back navigation between levels
4. Breadcrumb showing current path
5. Search/filter at each level
6. Selected module displayed with confirm option

Reuse:
- Import Dropdown from features/chat/ChatPage.tsx

State Management:
- Dropdown open/close state
- Current level (department/semester/subject/module)
- Hierarchy state (department_id, semester_id, subject_id, module_id)
- Search query state at each level
- Selected module persisted to local storage

Props Interface:
- onModuleSelect: Callback when module is selected (passes module object)
</action>
<verify>
1. Web-search: Research multi-level dropdown patterns and accessible combobox
2. Check syntax: Verify TypeScript compilation
3. Verify dropdown opens/closes correctly
4. Verify hierarchical navigation works
5. Verify selection persists
</verify>
<done>
ModuleSelector hierarchical component created with 4-level drill-down
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/06-frontend/06-02-SUMMARY.md`
</output>

<success_criteria>
- [ ] 8 files total (2 extended + 6 new)
- [ ] TypeScript types compile without errors
- [ ] Hierarchical drill-down: Department → Semester → Subject → Module
- [ ] ModuleSelector shows hierarchical navigation
- [ ] ModuleCard displays module with selection
- [ ] Selection persists across sessions
- [ ] React Query hooks fetch hierarchical data
- [ ] Dropdown component reused from ChatPage.tsx
- [ ] Only relevant modules shown (filtered by department)
- [ ] UI responsive on all screen sizes
</success_criteria>

<checkpoint:human-verify>
Test via browser:
1. Open module selector
2. Select department (only relevant depts shown)
3. Select semester (filtered by department)
4. Select subject (single select)
5. Select module (single select)
6. Confirm module selection persists
7. Test navigation back/forward between levels
</checkpoint:human-verify>

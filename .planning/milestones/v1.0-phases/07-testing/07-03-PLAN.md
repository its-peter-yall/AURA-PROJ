<objective>
Create comprehensive unit tests for AURA-NOTES-MANAGER frontend including KG processing components, Zustand store, and React Query hooks.
</objective>

<context>
@.planning/ROADMAP.md - Phase 7 (Testing & Optimization)
@AURA-NOTES-MANAGER/frontend/package.json - Has Vitest and testing-library
@AURA-NOTES-MANAGER/frontend/src/features/kg/ - KG Processing UI
@AURA-NOTES-MANAGER/frontend/src/stores/useExplorerStore.ts - Zustand store

Previous: Phase 7-02 AURA-CHAT Frontend Unit Tests
Current: Phase 7-03 AURA-NOTES-MANAGER Unit Tests
</context>

<requirements>
1. KGStatusBadge.test.tsx already exists (22 tests) - verify coverage
2. ProcessDialog.test.tsx already exists (~18 tests) - verify coverage
3. ProcessingQueue.test.tsx already exists (~20 tests) - verify coverage
4. Create useExplorerStore.test.ts (new)
5. Create ExplorerPage.test.tsx (new)
6. Create useKGProcessing.test.ts (new)
7. Coverage target: 75% for feature components, 90% for store
</requirements>

<tasks>
<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/features/kg/components/KGStatusBadge.test.tsx</parameter>
<action>
Verify existing KGStatusBadge tests cover all component behavior.

Current test file has 22 tests covering:
- Status rendering (pending, processing, ready, failed)
- Size variants (sm, md)
- Label visibility (showLabel true/false)
- Styling (color classes, custom className)
- Title attribute for tooltip
- Default values

Tests to ADD (if missing):
1. Tests for Clock icon in pending state (currently uses title check)
2. Tests for Loader2 icon in processing state (currently checks animate-spin class)
3. Tests for CheckCircle2 icon in ready state
4. Tests for XCircle icon in failed state

@see: features/kg/components/KGStatusBadge.tsx - Component under test
@see: existing test file at same location
</action>
<verify>
1. npm test -- KGStatusBadge.test.tsx -v
2. Verify all 22 tests pass
3. Check coverage report for component
</verify>
<done>
KGStatusBadge tests verified</done>
</task>

<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/features/kg/components/ProcessDialog.test.tsx</file>
<action>
Verify existing ProcessDialog tests cover all component behavior.

Current test file has ~18 tests covering:
- Dialog visibility (open/closed states)
- File count display
- Processing actions list (4 action items)
- Submit behavior (mutate call, loading state)
- Cancel behavior (close, overlay click)
- Success state (Documents Queued message)
- Error state (error display)

Tests to ADD (if missing):
1. Title changes to "Processing Started" on success
2. Button shows "Processing..." during loading
3. Zap icon present on submit button
4. Done button calls clearSelection and setSelectionMode(false)

@see: features/kg/components/ProcessDialog.tsx - Component under test
@see: existing test file at same location
</action>
<verify>
1. npm test -- ProcessDialog.test.tsx -v
2. Verify all tests pass
3. Check coverage report
</verify>
<done>
ProcessDialog tests verified</done>
</task>

<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/features/kg/components/ProcessingQueue.test.tsx</file>
<action>
Verify existing ProcessingQueue tests cover all component behavior.

Current test file has ~20 tests covering:
- Visibility states (loading, empty, undefined, error)
- Queue display (header, count, file names)
- Progress display (percentage, step text)
- Status colors (processing, failed, ready)
- Error display per item

Tests to ADD (if missing):
1. Fixed position container (bottom-right)
2. Polling spinner in header (Loader2 with animate-spin)
3. Progress bar width matches item.progress
4. max-h-60 overflow on queue items
5. Last item has no border
6. AlertCircle icon for error display

@see: features/kg/components/ProcessingQueue.tsx - Component under test
@see: existing test file at same location
</action>
<verify>
1. npm test -- ProcessingQueue.test.tsx -v
2. Verify all tests pass
3. Check coverage report
</verify>
<done>
ProcessingQueue tests verified</done>
</task>

<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/stores/useExplorerStore.test.ts</parameter>
<action>
Create unit tests for Zustand store (useExplorerStore).

IMPORTANT: Store has MORE actions than originally planned:
- setClipboard (sets nodeIds and mode)
- clearClipboard (resets clipboard)
- setRenamingNodeId (sets/clears renaming state)
- creatingParentId (set by startCreating, cleared by cancelCreating/navigateTo/navigateUp)
- contextMenuNodeId (set by openContextMenu, cleared by closeContextMenu)
- lastSelectedId (updated by select, toggleSelect, rangeSelect, selectAll)
- navigateTo takes BOTH node AND ancestors parameters

Initial state tests:
1. currentPath === []
2. selectedIds === new Set()
3. lastSelectedId === null
4. viewMode === 'grid'
5. searchQuery === ''
6. expandedIds === new Set()
7. clipboard.nodeIds === [], clipboard.mode === null
8. contextMenuPosition === null, contextMenuNodeId === null
9. renamingNodeId === null
10. creatingNodeType === null, creatingParentId === null
11. warningDialog.isOpen === false
12. warningTimeoutId === null
13. selectionMode === false
14. kgPolling.moduleId === null, kgPolling.isPolling === false
15. processDialog.open === false, fileIds=[], moduleId=''

Navigation action tests:
16. setActiveNode with node updates activeNodeId
17. setActiveNode with null clears activeNodeId
18. setCurrentPath replaces entire path
19. navigateTo with node and ancestors builds path
20. navigateUp from multi-node path removes last node
21. navigateUp from single node returns to root (path=[])
22. navigateTo clears selection and creating state
23. navigateUp clears selection and creating state

Selection action tests:
24. select replaces selection with single item, sets lastSelectedId
25. toggleSelect adds item not in selection
26. toggleSelect removes item in selection
27. rangeSelect with no lastSelectedId selects single item
28. rangeSelect with valid range selects multiple items
29. rangeSelect handles reverse selection (end < start)
30. clearSelection empties set, clears lastSelectedId
31. selectAll adds all IDs, sets lastSelectedId to last
32. lastSelectedId updates on select/toggleSelect/rangeSelect

Tree expansion tests:
33. expand adds ID to expandedIds
34. collapse removes ID from expandedIds
35. toggleExpand adds when collapsed
36. toggleExpand removes when expanded

Clipboard action tests:
37. setClipboard sets nodeIds and mode
38. clearClipboard resets to empty nodeIds and null mode

Context menu tests:
39. openContextMenu sets position and nodeId
40. closeContextMenu clears position and nodeId
41. openDeleteDialog sets nodeToDelete, closes context menu
42. closeDeleteDialog clears nodeToDelete

View action tests:
43. setViewMode updates viewMode
44. setSearchQuery updates searchQuery

Rename tests:
45. setRenamingNodeId with ID sets state
46. setRenamingNodeId with null clears state

Inline creation tests:
47. startCreating sets type and parentId
48. cancelCreating clears both type and parentId

Warning dialog tests:
49. openWarningDialog sets isOpen=true
50. closeWarningDialog sets isOpen=false
51. warningTimeoutId is cleared on close

Process dialog tests:
52. openProcessDialog sets open=true, fileIds, moduleId
53. closeProcessDialog resets to open=false

KG polling tests:
54. setKGPolling sets moduleId and isPolling

Testing approach: Use create() to get store creator, create fresh instance per test.

@see: stores/useExplorerStore.ts - Store under test
</action>
<verify>
1. npm test -- useExplorerStore.test.ts -v
2. Verify 54+ tests pass (covering all store actions)
3. Check store coverage (should be >90%)
</verify>
<done>
54 Zustand store unit tests created</done>
</task>

<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/features/kg/hooks/useKGProcessing.test.ts</file>
<action>
Create unit tests for useKGProcessing hook.

Tests for useFileKGStatus:
1. returns data from query
2. returns isLoading state
3. returns error state
4. enabled flag requires documentId

Tests for useProcessingQueue:
5. returns queue data
6. returns isLoading state
7. returns error state
8. refetchInterval returns 2000 when items are processing
9. refetchInterval returns false when no processing items
10. polls every 2s when queue has active items

Tests for processFiles mutation:
11. mutate accepts file_ids and module_id payload
12. mutate calls processKGBatch API
13. onSuccess invalidates kg queue queries
14. onSuccess calls setKGPolling with module_id
15. onError sets error state
16. mutate function is callable

Mock API: Mock @tanstack/react-query's useQuery and useMutation.
Mock store: Mock useExplorerStore for setKGPolling.

@see: features/kg/hooks/useKGProcessing.ts - Hook under test
@see: api/explorerApi.ts - API functions (processKGBatch, getKGProcessingQueue, getKGDocumentStatus)
</action>
<verify>
1. npm test -- useKGProcessing.test.ts -v
2. Verify 16+ tests pass
3. Check hook coverage
</verify>
<done>
16 useKGProcessing hook tests created</done>
</task>

<task>
<type>verify</type>
<file>AURA-NOTES-MANAGER/frontend/src/pages/ExplorerPage.test.tsx</parameter>
<action>
Create unit tests for ExplorerPage component.

IMPORTANT: FileSelectionBar returns null (functionality moved to Header)
- REMOVE test for "Selection bar visible" - it renders nothing

Render tests:
1. renders explorer-layout class on main container
2. renders Sidebar component
3. renders Header component
4. shows loading spinner during fetch (isLoading=true)
5. displays error state when query fails
6. shows empty state when folder empty
7. shows "Create a department" hint at root
8. shows "Right-click to create" hint for nested folders
9. renders GridView when viewMode=grid
10. renders ListView when viewMode=list
11. passes children items to GridView
12. passes children items to ListView

Dialog tests:
13. shows ConfirmDialog when deleteDialogOpen=true
14. shows WarningDialog component
15. renders ContextMenu when contextMenuPosition is set

KG feature tests:
16. renders ProcessDialog component (even when closed)
17. renders ProcessingQueue component (even when empty)
18. FileSelectionBar renders nothing (returns null) - verify NOT visible

Interaction tests:
19. background click closes context menu
20. confirm delete calls correct API based on type
21. refetchQueries called after successful delete
22. window.__auraStore set in DEV mode

Mock API: Mock useQuery, useQueryClient, and api functions.
Mock store: Mock useExplorerStore for state values.

@see: pages/ExplorerPage.tsx - Component under test
@note: FileSelectionBar.tsx returns null - skip visibility test
</action>
<verify>
1. npm test -- ExplorerPage.test.tsx -v
2. Verify 22 tests pass
3. Check page coverage
</verify>
<done>
22 ExplorerPage unit tests created</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/07-testing/07-03-SUMMARY.md`
</output>

<success_criteria>
- [ ] 3 test files verified (already exist: KGStatusBadge, ProcessDialog, ProcessingQueue)
- [ ] 3 new test files created (useExplorerStore, useKGProcessing, ExplorerPage)
- [ ] 96+ total tests (22 + 18 + 20 + 54 + 16 + 22)
- [ ] 75%+ feature component coverage
- [ ] 90%+ store coverage
- [ ] All tests pass
</success_criteria>

<checkpoint:human-verify>
1. Run: cd AURA-NOTES-MANAGER/frontend && npm test -- --run
2. Verify test count (96+) and pass rate (100%)
3. Check coverage report for each component
4. Review test file quality and assertions
</checkpoint>

<edge_cases_covered>
1. Empty queue auto-hide (ProcessingQueue returns null)
2. Error states in all components
3. Loading states with spinners
4. Success state transitions (ProcessDialog)
5. Selection mode cleanup after close
6. Range selection with edge indices (forward and reverse)
7. Path navigation edge cases (root, single node, multi-node)
8. Toggle expansion on expanded/collapsed nodes
9. Warning dialog auto-timeout handling (timeout cleared on close)
10. Dialog close during processing state
11. lastSelectedId updated by all selection operations
12. creatingParentId cleared on navigation
13. contextMenuNodeId set/cleared with position
14. Polling behavior when queue has/doesn't have active items
15. FileSelectionBar returns null (no rendering)
</edge_cases_covered>

<store_actions_verified>
Navigation: setActiveNode, setCurrentPath, navigateTo, navigateUp
Selection: select, toggleSelect, rangeSelect, clearSelection, selectAll
Tree: expand, collapse, toggleExpand
View: setViewMode, setSearchQuery
Clipboard: setClipboard, clearClipboard
Context: openContextMenu, closeContextMenu
Delete: openDeleteDialog, closeDeleteDialog
Rename: setRenamingNodeId
Creation: startCreating, cancelCreating
Warning: openWarningDialog, closeWarningDialog
Process: openProcessDialog, closeProcessDialog
KG: setKGPolling
Mode: setSelectionMode
</store_actions_verified>

<known_issues_from_codebase>
1. FileSelectionBar.tsx returns null - tests should verify null, not visibility
2. navigateTo takes TWO parameters (node, ancestors) not one
3. rangeSelect merges new range with existing selectedIds (not replaces)
4. toggleExpand calls get().expand() or get().collapse() internally
5. warningTimeoutId must be cleared when closeWarningDialog is called
6. openDeleteDialog clears context menu state
7. useProcessingQueue has smart polling (2000ms only when processing)
8. processFiles onSuccess calls setKGPolling(variables.module_id, true)
</known_issues_from_codebase>

```
<objective>
Create SessionManager class in AURA-CHAT/backend/session_manager.py for persistent chat sessions with Neo4j storage and Redis caching. Supports session CRUD operations, message storage with ordering, and session lifecycle management.
</objective>

<context>
@.planning/ROADMAP.md - Phase 5 (Study Session System)
@AURA-CHAT/backend/graph_manager.py - Existing graph operations (execute_query method)
@AURA-CHAT/backend/routers/student_modules.py - Redis caching pattern (lines 154-180)
@AURA-NOTES-MANAGER/api/module_manager.py - Manager class pattern (reference)

Previous: Phase 4 AURA-CHAT Module Integration complete
Current: Session persistence layer
</context>

<requirements>
1. SessionManager class with Neo4j for persistence and Redis for caching
2. StudySession node CRUD operations with user ownership validation
3. Message storage using HAS_MESSAGE relationship with message_order property for ordering
4. Redis caching for active sessions with 24-hour TTL
5. User ownership validation on all session operations

Session Data Model:
- StudySession nodes with: id, title, module_ids, user_id, status, message_count, created_at, updated_at
- Message nodes with: id, session_id, role, content, created_at, model_used, sources, thinking_content, token_count
- HAS_MESSAGE relationship with message_order property for ordering messages

Redis cache pattern:
- Key format: "session:{session_id}"
- TTL: 24 hours (86400 seconds)
- Stores active session metadata for fast access
</requirements>

<tasks>
<task>
<type>create</type>
<file>AURA-CHAT/backend/session_manager.py</file>
<action>
Create session_manager.py with SessionManager class.

Purpose:
- Manage study sessions (create, read, update, delete) stored in Neo4j
- Cache active sessions in Redis for fast retrieval
- Provide message storage with ordering for chat history

Class Structure:
1. Initialize with graph_manager and Redis connection parameters
2. Define constants: SESSION_PREFIX for Redis keys, MESSAGE_LIMIT for pagination

Session CRUD Methods:
- create_session(user_id, title, module_ids): Creates StudySession node, caches in Redis, returns session dict
- get_session(session_id): Tries Redis cache first, falls back to Neo4j, caches result
- list_user_sessions(user_id, status=None): Returns all user sessions ordered by updated_at DESC
- update_session(session_id, **kwargs): Updates session properties, invalidates cache
- delete_session(session_id): Deletes session and all messages (DETACH DELETE), removes from cache
- validate_ownership(session_id, user_id): Returns True if user owns the session

Message Methods:
- add_message(session_id, role, content, model_used, sources, thinking_content, token_count):
  Creates Message node with HAS_MESSAGE relationship, increments session message_count, returns message dict
- get_messages(session_id, limit=100, offset=0): Returns messages ordered by created_at ASC with pagination
- get_message_count(session_id): Returns total message count from session metadata

Session Lifecycle Methods:
- archive_session(session_id): Updates status to 'archived'
- resume_session(session_id): Updates status to 'active'

Redis Caching Methods:
- _cache_session(session_id, session): Stores session in Redis with 24h TTL
- _get_cached_session(session_id): Retrieves session from Redis cache
- _uncache_session(session_id): Removes session from Redis cache

Error Handling:
- Log all errors with meaningful messages
- Return None for not-found operations
- Raise exceptions for critical failures (connection errors)
</action>
<verify>
1. Web-search: Research Redis Python client patterns and Neo4j session management best practices
2. Check syntax: `python -m py_compile AURA-CHAT/backend/session_manager.py`
3. Verify SessionManager class has all CRUD methods
4. Verify Redis caching methods exist
</verify>
<done>
SessionManager class with CRUD, messaging, and caching methods created
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/05-study-session/05-01-SUMMARY.md`
</output>

<success_criteria>
- [ ] SessionManager class with 10+ methods
- [ ] create_session() creates StudySession node in Neo4j
- [ ] add_message() creates Message node with HAS_MESSAGE relationship
- [ ] get_messages() returns ordered message history
- [ ] Redis caching for active sessions with TTL
- [ ] User ownership validation via validate_ownership()
- [ ] All files pass py_compile check
</success_criteria>

<checkpoint:human-verify>
Test SessionManager via Python:
```python
from backend.session_manager import SessionManager
from backend.graph_manager import GraphManager
gm = GraphManager()
sm = SessionManager(gm)
session = sm.create_session("user123", "Study Session", ["mod_cs_001"])
print(session)
```
</checkpoint:human-verify>

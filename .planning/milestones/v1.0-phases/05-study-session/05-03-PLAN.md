```
<objective>
Add user analytics methods to SessionManager and integrate sessions router into AURA-CHAT backend main.py with proper initialization. Also export sessions router from routers/__init__.py.
</objective>

<context>
@.planning/ROADMAP.md - Phase 5 (Study Session System)
@AURA-CHAT/backend/session_manager.py - SessionManager (05-01, extend)
@AURA-CHAT/backend/routers/sessions.py - Sessions router (05-02, integrate)
@AURA-CHAT/backend/routers/__init__.py - Router exports (update)
@AURA-CHAT/server/main.py - App entry point (update)

Previous: 05-01 SessionManager, 05-02 Sessions router complete
Current: Analytics and integration
</context>

<requirements>
1. Add user analytics methods to SessionManager class
2. Add analytics endpoints to sessions router
3. Export sessions router from routers/__init__.py
4. Integrate sessions router into server/main.py
5. Initialize SessionManager and RAGEngine for sessions router

User Analytics Methods:
- get_user_analytics(user_id): Aggregate stats across all user sessions
- get_user_session_count(user_id): Total session count
- get_user_message_count(user_id): Total message count
- get_user_module_usage(user_id): Modules most studied by user (sorted by frequency)
- get_session_activity_timeline(session_id): Message activity by hour
- get_recent_sessions(user_id, limit=5): Most recently active sessions

Router Integration:
- Export sessions router from routers package
- Initialize SessionManager in main.py
- Initialize sessions router with SessionManager and RAGEngine
- Include sessions router in FastAPI app
</requirements>

<tasks>
<task>
<type>update</type>
<file>AURA-CHAT/backend/session_manager.py</file>
<action>
Add user analytics methods to SessionManager class.

Purpose:
- Provide aggregate analytics across all user sessions
- Track module usage patterns
- Support activity timeline visualization

Methods to Add:

1. get_user_analytics(user_id) -> Dict[str, Any]:
   - Aggregates statistics across all user sessions
   - Returns: total_sessions, total_messages, active_sessions, completed_sessions, top_modules
   - top_modules is list of {module_id, session_count} sorted by usage

2. get_user_session_count(user_id) -> int:
   - Returns total number of sessions for user

3. get_user_message_count(user_id) -> int:
   - Returns total message count across all user sessions

4. get_user_module_usage(user_id) -> List[Dict[str, Any]]:
   - Returns list of {module_id, session_count} sorted by usage frequency
   - Shows which modules user studies most

5. get_session_activity_timeline(session_id) -> List[Dict[str, Any]]:
   - Returns message activity by hour of day
   - Format: [{hour: int, message_count: int}]
   - Used for activity visualization

6. get_recent_sessions(user_id, limit=5) -> List[Dict[str, Any]]:
   - Returns most recently active sessions
   - Ordered by updated_at DESC
   - Limited to specified count

All methods should handle errors gracefully and return meaningful defaults or empty structures.
</action>
<verify>
1. Web-search: Research Python analytics patterns, data aggregation strategies, and timeline visualization approaches
2. Check syntax: `python -m py_compile AURA-CHAT/backend/session_manager.py`
3. Verify all new analytics methods exist
4. Methods should be callable without errors
</verify>
<done>
User analytics methods added to SessionManager
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/backend/routers/sessions.py</file>
<action>
Add user analytics endpoints to sessions router.

Purpose:
- Expose user-level analytics via REST API
- Provide module usage insights
- Support recent session listing

Endpoints to Add:

1. GET /api/sessions/analytics/user/{user_id} - User Analytics
   - Returns: UserAnalyticsResponse with all aggregated stats
   - No ownership validation needed (user sees their own data)

2. GET /api/sessions/analytics/user/{user_id}/modules - Module Usage
   - Returns: List of ModuleUsageResponse [{module_id, session_count}]
   - Sorted by session_count DESC

3. GET /api/sessions/analytics/user/{user_id}/recent - Recent Sessions
   - Query: limit (int, default 5)
   - Returns: {sessions: List[SessionResponse], count: int}
   - Shows most recently active sessions

Schemas to Add:
- UserAnalyticsResponse: user_id, total_sessions, total_messages, active_sessions, completed_sessions, top_modules
- ModuleUsageResponse: module_id, session_count
</action>
<verify>
1. Web-search: Research FastAPI analytics endpoint patterns and response schema best practices
2. Check syntax: `python -m py_compile AURA-CHAT/backend/routers/sessions.py`
3. Verify 3 new analytics endpoints have proper decorators
</verify>
<done>
User analytics endpoints added to sessions router
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/backend/routers/__init__.py</file>
<action>
Update routers package init to export sessions router.

Purpose:
- Make sessions router available via package import

Current exports: student_modules
Add: sessions

Result:
```python
from .student_modules import router as student_modules
from .sessions import router as sessions

__all__ = ["student_modules", "sessions"]
```
</action>
<verify>
1. Web-search: Research Python package export patterns and FastAPI router inclusion best practices
2. Check syntax: `python -m py_compile AURA-CHAT/backend/routers/__init__.py`
</verify>
<done>
Sessions router exported from package
</done>
</task>

<task>
<type>update</type>
<file>AURA-CHAT/server/main.py</file>
<action>
Add sessions router integration to main.py.

Purpose:
- Initialize SessionManager with GraphManager
- Pass SessionManager and RAGEngine to sessions router
- Include sessions router in FastAPI app

Steps:
1. Import SessionManager from backend.session_manager
2. Import sessions router and initialization functions from backend.routers.sessions
3. Create GraphManager instance (if not already present)
4. Create SessionManager instance with GraphManager
5. Call initialize_session_manager(session_manager) to set the global
6. Call initialize_rag_engine(rag_engine) to pass RAG engine (if rag_engine exists)
7. Include sessions router: app.include_router(sessions.router, prefix="/api")

Integration Points:
- Find existing student_modules inclusion
- Add sessions router nearby for consistency
- Ensure SessionManager initialization happens before app starts
</action>
<verify>
1. Web-search: Research FastAPI app initialization patterns and global state management
2. Check syntax: `python -m py_compile AURA-CHAT/server/main.py`
3. Verify sessions router is included in app
</verify>
<done>
Sessions router integrated into main app
</done>
</task>
</tasks>

<output>
SUMMARY.md in `.planning/phases/05-study-session/05-03-SUMMARY.md`
</output>

<success_criteria>
- [ ] SessionManager has 16+ methods (original 10 + 6 new analytics)
- [ ] 11 total endpoints (8 original + 3 analytics)
- [ ] GET /api/sessions/analytics/user/{user_id} returns user analytics
- [ ] GET /api/sessions/analytics/user/{user_id}/modules returns module usage
- [ ] Sessions router exported from routers/__init__.py
- [ ] Sessions router integrated in server/main.py
- [ ] All files pass py_compile check
</success_criteria>

<checkpoint:human-verify>
Test complete Phase 5 via Swagger UI:
1. Create session: POST /api/sessions
2. Add query: POST /api/sessions/{id}/query
3. Check analytics: GET /api/sessions/analytics/user/{user_id}
4. View stats: GET /api/sessions/{id}/stats
</checkpoint:human-verify>

# Requirements Archive: v1.2 Settings Wiring E2E

**Shipped:** 2026-03-23
**Requirements:** 12/12 satisfied

## Requirements

### API Configuration

- [x] **API-01**: Admin can configure gatekeeper model/provider via settings page (add `"gatekeeper"` to `ALLOWED_USE_CASES` in both settings routers + frontend type + UI)
  - **Outcome:** VALIDATED — Both AURA-CHAT and NOTES-MANAGER settings routers accept `gatekeeper`; frontend types and UI rows match
  - **Verified by:** Phase 14 (14-02-PLAN.md)

- [x] **API-02**: Admin can configure relationship extraction model/provider via settings page (add `"relationship_extraction"` to `ALLOWED_USE_CASES` in both settings routers + frontend type + UI)
  - **Outcome:** VALIDATED — Both routers accept `relationship_extraction`; frontend types and UI rows match
  - **Verified by:** Phase 14 (14-02-PLAN.md)

### Provider Passthrough — AURA-CHAT

- [x] **PP-01**: Entity extractor passes explicit `provider` from SettingsStore to `GenerateRequest` (remove reliance on `/` heuristic in `AURA-CHAT/backend/llm_entity_extractor.py`)
  - **Outcome:** VALIDATED — `resolve_use_case_config("entity_extraction")` called at init; provider passed to `get_model(provider=)`
  - **Verified by:** Phase 15 (15-02-PLAN.md)

- [x] **PP-02**: Gatekeeper routes through ModelRouter with explicit provider (remove OpenRouter blanket skip in `AURA-CHAT/backend/llm_gatekeeper.py:153-159`, handle JSON mode per-provider)
  - **Outcome:** VALIDATED — OpenRouter skip removed; `resolve_use_case_config("gatekeeper")` used; JSON mode translation in OpenRouter provider
  - **Verified by:** Phase 15 (15-01-PLAN.md, 15-03-PLAN.md)

- [x] **PP-03**: Embeddings passes `provider` from SettingsStore to `router.embed()` consistently (`AURA-CHAT/backend/utils/embeddings.py`)
  - **Outcome:** VALIDATED — VERTEX_PROJECT check made provider-aware; `_embedding_default.get("provider")` used
  - **Verified by:** Phase 15 (15-03-PLAN.md)

- [x] **PP-04**: Relationship extraction reads from SettingsStore with env var fallback, passes `provider` to ModelRouter (`AURA-CHAT/backend/`)
  - **Outcome:** VALIDATED — `_resolve_relationship_model()` calls `resolve_use_case_config("relationship_extraction")` independently
  - **Verified by:** Phase 15 (15-02-PLAN.md)

### Provider Passthrough — AURA-NOTES-MANAGER

- [x] **PP-05**: KG processor reads from SettingsStore at runtime (replace `LLM_ENTITY_EXTRACTION_MODEL` env var constant with `resolve_use_case_config("entity_extraction")` in `AURA-NOTES-MANAGER/api/kg_processor.py`)
  - **Outcome:** VALIDATED — `generate_text()` and `extract_entities()` both call `resolve_use_case_config("entity_extraction")` at method start
  - **Verified by:** Phase 16 (16-01-PLAN.md)

- [x] **PP-06**: Entity extractor passes explicit `provider` from SettingsStore (`AURA-NOTES-MANAGER/services/llm_entity_extractor.py`)
  - **Outcome:** VALIDATED — `resolve_use_case_config("entity_extraction")` called at lines 453, 959; provider passed to `router.generate(provider=)`
  - **Verified by:** Phase 16 (16-01-PLAN.md)

- [x] **PP-07**: Embeddings passes `provider` from SettingsStore to `router.embed()` (`AURA-NOTES-MANAGER/services/embeddings.py`)
  - **Outcome:** VALIDATED — `_embed_batch_sync()` resolves config at each batch; `provider=cfg["provider"]` to `router.embed()`
  - **Verified by:** Phase 16 (16-02-PLAN.md)

- [x] **PP-08**: Summarizer routes through ModelRouter instead of direct Vertex SDK calls (`AURA-NOTES-MANAGER/services/summarizer.py`)
  - **Outcome:** VALIDATED — `router.generate()` replaces dual SDK path; `logger.warning(..., exc_info=True)` replaces bare `except:pass`
  - **Verified by:** Phase 16 (16-02-PLAN.md)

### Fallback Behavior

- [x] **FB-01**: SettingsStore value is authoritative over env vars when SettingsStore is reachable (fix `AURA-NOTES-MANAGER/api/config.py` env var shadowing in `kg_processor.py`)
  - **Outcome:** VALIDATED — `resolve_use_case_config()` checks SettingsStore first, env vars second, hardcoded default third
  - **Verified by:** Phase 14 (14-01-PLAN.md)

- [x] **FB-02**: Graceful degradation documented and verified — log warning on Redis down, fall back to env vars, never crash (verify `get_default_sync` exception handling across all consumers)
  - **Outcome:** VALIDATED — `_SENTINEL_ERROR` cached with 30s TTL on Redis failure; `logger.warning` logged; consumer continues with fallback
  - **Verified by:** Phase 14 (14-01-PLAN.md)

## Traceability

| Requirement | Phase | Plan | Status |
|-------------|-------|------|--------|
| API-01 | Phase 14 | 14-02 | Complete |
| API-02 | Phase 14 | 14-02 | Complete |
| PP-01 | Phase 15 | 15-02 | Complete |
| PP-02 | Phase 15 | 15-01, 15-03 | Complete |
| PP-03 | Phase 15 | 15-03 | Complete |
| PP-04 | Phase 15 | 15-02 | Complete |
| PP-05 | Phase 16 | 16-01 | Complete |
| PP-06 | Phase 16 | 16-01 | Complete |
| PP-07 | Phase 16 | 16-02 | Complete |
| PP-08 | Phase 16 | 16-02 | Complete |
| FB-01 | Phase 14 | 14-01 | Complete |
| FB-02 | Phase 14 | 14-01 | Complete |

**Final Coverage:** 12/12 — All requirements satisfied

## Deferred to Future Milestones

- **FUT-01**: Dynamic thinking mode model list from provider capabilities metadata
- **FUT-02**: Multi-provider chat config fallback (include OpenRouter models when Redis is down)
- **FUT-03**: SettingsStore health indicator in settings page UI

---

_Requirements defined: 2026-03-23_
_Archived: 2026-03-23 after v1.2 milestone_

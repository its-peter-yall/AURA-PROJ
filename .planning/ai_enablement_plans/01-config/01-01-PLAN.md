# 01-01: Create api/config.py

**Phase:** 01-config
**Task Type:** Configuration
**Context:** @AI_ENABLEMENT_ROADMAP.md

## Objective

Create centralized `api/config.py` with LLM model settings, Google Cloud credentials, Neo4j connection, and Redis configuration. This file mirrors `AURA-CHAT/backend/utils/config.py` pattern.

## Actions

### 1. Check existing config.py

```bash
# Check if file exists and read contents
cat AURA-NOTES-MANAGER/api/config.py 2>/dev/null || echo "File does not exist"
```

### 2. Create/Update config.py with required settings

**File:** `AURA-NOTES-MANAGER/api/config.py`

Add these environment variable definitions:

```python
import os
from pathlib import Path
from dotenv import load_dotenv

# Load environment variables from .env
dotenv_path = Path(__file__).parent.parent / ".env"
if dotenv_path.exists():
    load_dotenv(dotenv_path)

# Google Cloud / Vertex AI Configuration
VERTEX_PROJECT = os.getenv("VERTEX_PROJECT", "lucky-processor-480412-n8")
VERTEX_LOCATION = os.getenv("VERTEX_LOCATION", "us-central1")
VERTEX_CREDENTIALS = os.getenv("VERTEX_CREDENTIALS", "AURA-NOTES-MANAGER/service_account.json")
GOOGLE_APPLICATION_CREDENTIALS = os.getenv("GOOGLE_APPLICATION_CREDENTIALS", VERTEX_CREDENTIALS)

# Set GOOGLE_APPLICATION_CREDENTIALS for Vertex AI SDK
os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = GOOGLE_APPLICATION_CREDENTIALS

# LLM Model Configuration
LLM_ENTITY_EXTRACTION_MODEL = "gemini-2.5-flash-lite"
LLM_SUMMARIZATION_MODEL = "gemini-2.5-flash-lite"
EMBEDDING_MODEL = "text-embedding-004"

# Test Mode (set to True to skip actual API calls)
AURA_TEST_MODE = os.getenv("AURA_TEST_MODE", "false").lower() == "true"

# Neo4j Configuration
NEO4J_URI = os.getenv("NEO4J_URI", "bolt://127.0.0.1:7687")
NEO4J_USER = os.getenv("NEO4J_USER", "neo4j")
NEO4J_PASSWORD = os.getenv("NEO4J_PASSWORD", "password")

# Redis Configuration
REDIS_URL = os.getenv("REDIS_URL", "redis://127.0.0.1:6379/0")

# Celery Configuration
CELERY_RESULT_EXPIRES = int(os.getenv("CELERY_RESULT_EXPIRES", "3600"))
```

### 3. Update .env file with required variables

**File:** `AURA-NOTES-MANAGER/.env`

Add/Update these variables:

```bash
# Vertex AI Configuration
VERTEX_PROJECT=lucky-processor-480412-n8
VERTEX_LOCATION=us-central1
VERTEX_CREDENTIALS=AURA-NOTES-MANAGER/service_account.json

# Set this to "true" for testing without API calls
AURA_TEST_MODE=false

# Neo4j Configuration
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password_here

# Redis Configuration
REDIS_URL=redis://127.0.0.1:6379/0
```

### 4. Verify config.py works

```bash
cd AURA-NOTES-MANAGER
../../.venv/Scripts/python -c "
from api.config import (
    LLM_ENTITY_EXTRACTION_MODEL,
    EMBEDDING_MODEL,
    VERTEX_PROJECT,
    NEO4J_URI,
    REDIS_URL,
    AURA_TEST_MODE
)
print(f'LLM Entity Extraction Model: {LLM_ENTITY_EXTRACTION_MODEL}')
print(f'Embedding Model: {EMBEDDING_MODEL}')
print(f'Vertex Project: {VERTEX_PROJECT}')
print(f'Neo4j URI: {NEO4J_URI}')
print(f'Redis URL: {REDIS_URL}')
print(f'Test Mode: {AURA_TEST_MODE}')
"
```

## Verify

- [ ] `api/config.py` exists
- [ ] Config imports work without errors
- [ ] All required constants defined (LLM_MODEL, EMBEDDING_MODEL, VERTEX_PROJECT, NEO4J_URI, REDIS_URL)
- [ ] `.env` file updated with required variables

## Done

When `api/config.py` exists and all imports work correctly. Run the verification command and ensure output shows all configuration values.

## Checkpoints

- None required - straightforward configuration task
